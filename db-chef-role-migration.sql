-- ─── Migration: اضافه کردن نقش Chef به enum user_role ───────────────────────
-- تاریخ: 2026-06-18
-- توضیح: نقش سرآشپز با دسترسی انبار/آشپزخانه، رسپی، food cost — بدون اطلاعات مالی
--
-- نکته: PostgreSQL از ALTER TYPE ... ADD VALUE پشتیبانی می‌کند (نسخه ۹.۱+).
--        این دستور ایمن است و روی ردیف‌های موجود تأثیر نمی‌گذارد.
-- ─────────────────────────────────────────────────────────────────────────────

ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'Chef';
