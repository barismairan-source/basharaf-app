# HANDOFF.md — دفتر وضعیت زنده‌ی پروژه «با شرف»

> قانون طلایی: **هر جلسه اول بخش ۰ را بخوان؛ آخر جلسه بخش ۰ + ژورنال را به‌روز کن و commit/push کن.**
> جزئیات تاریخی قدیمی‌تر: `project-docs/handoff-archive.md`.

---

## ⚡ بخش ۰ — وضعیت لحظه‌ای (اول این را بخوان)

| | |
|---|---|
| **نسخه** | `main` (v0.31.0) — کل `feat/product-ui-v2` (پایه‌ی UI مشترک + بازطراحی داشبورد + بازطراحی لیست تراکنش‌ها + بازطراحی فرم «ثبت تراکنش») merge/deploy شده. branch جداگانه‌ای در دست نیست. |
| **آخرین به‌روزرسانی** | 2026-07-25 |
| **Build/tsc** | روی `main` (commit `59af832`): tsc ✅ ۰ خطا · tests 393/393 ✅ · build ✅ · lint ✅ · playwright --list ✅ |
| **دیپلوی** | ✅ main تا `59af832` دیپلوی شده — GH Actions run `30168916747`، attempt اول، موفق. |
| **کار نیمه‌تمام (in-progress)** | — (کل فاز «Product UI V2» — پایه، داشبورد، تراکنش‌ها، فرم ثبت تراکنش — merge/deploy شد؛ بررسی بصری authenticated واقعی روی production هنوز انجام نشده). |
| **کار بعدی پیشنهادی** | از کاربر بخواه یک تراکنش واقعی (هزینه/درآمد/انتقال) از فرم جدید `/transactions/new` روی سایت زنده امتحان کنه. بعدش: مهاجرت تدریجی بقیه صفحات به `PageShell`/`FilterToolbar`/`MetricGrid` (هنوز شروع نشده). |
| **بلاک‌شده/منتظر کاربر** | ۱) Playwright e2e ⛔ — `.env.e2e` غایب (بدون تغییر، چند ماهه). ۲) منشأ `below_approval_limit.email_enabled=true` (از قبل production) هنوز چک نشده. ۳) Phase 2 قدیمی (`UI-UX-REFRESH-PHASE-1.md`): ۱۸ دکمه‌ی آیکونی باقیمانده، مهاجرت رنگ dark پنل ادمین — هنوز روی میز. |

> ⚠️ **نکته مهم برای جلسات بعدی:** فرم `/apply` حالا کاملاً داینامیک و دیتابیس‌محور است. **دیگر فیلد hard-code به `app/apply/page.tsx` یا `lib/recruitment/` اضافه نکنید.** همه فیلدهای جدید باید از طریق `/recruitment/form-builder` ایجاد شوند.

---

## 📋 پروتکل جلسه

### شروع هر جلسه (الزامی — به ترتیب)
⛔ **تنها یک نشست فعال Claude Code مجاز است روی این پوشه ویرایش کند — دو نشست همزمان باز نکن.**
1. این فایل، بخش ۰ + جدیدترین ورودی ژورنال را بخوان.
2. `git status` بزن — اگر تغییرات commit‌نشده هست، یعنی جلسه‌ی قبل ناقص بسته شده: اول وضعیت را با کاربر روشن کن، چیزی را کورکورانه commit یا revert نکن.
3. `git log -5 --oneline` — تطبیق بده با ژورنال.
4. به کاربر خلاصه بگو: «وضعیت X است، کار نیمه‌تمام Y، پیشنهاد بعدی Z» و منتظر تأیید بمان.

### پایان هر جلسه / بعد از هر تغییر معنادار (الزامی)
1. یک ورودی ژورنال با **قالب زیر** به بالای بخش ژورنال اضافه کن.
2. بخش ۰ را به‌روز کن (نسخه، تاریخ، کار نیمه‌تمام، کار بعدی).
3. اگر ژورنال بیش از **۷ ورودی** شد، قدیمی‌ها را به `project-docs/handoff-archive.md` منتقل کن.
4. `git add -A && git commit -m "..." && git push` — **بدون push جلسه را نبند.**
5. ⛔ **دیگر ZIP نساز** — GitHub Actions خودکار deploy می‌کند (از 2026-06-24).

### قالب اجباری ورودی ژورنال
```markdown
## 📓 [تاریخ] — [عنوان کوتاه]
**چه شد:** (۲–۵ خط، تصمیم‌های مهم + چرایی)
**فایل‌ها:** (مسیر کامل ایجاد/ویرایش‌شده‌ها)
**Build:** tsc/build سبز یا خطا + متن خطا
**ناتمام:** دقیقاً کجا متوقف شد، چه چیزی نیمه‌کاره است (اگر هیچ: «—»)
**برای جلسه‌ی بعد:** کار بعدی مشخص + هر هشداری که جلسه‌ی بعد باید بداند
```

---

## 🏗 بخش ۴ — مرجع پایدار (قبل از دست‌زدن به مالی بخوان)

- `accounts.balance` فیلد **cache** است: همیشه از reverse transaction‌ها محاسبه شده. هر تراکنش معکوس جا مانده = drift دائمی.
- ⚠ **INVARIANT فاز ۵**: `accounts.balance` هرگز توسط فیلتر `is_setup` (لنز عملیاتی) تغییر نمی‌کند. این فیلتر فقط روی KPI‌های جریان مالی (income/expense/profit) در داشبورد و گزارش‌ها اثر دارد — ترازنامه مستقل است.
- پول **bigint تومان** در DB؛ در TypeScript `number` (JS safe integer تا ۹۰۰۰ تریلیون کافی است).
- تاریخ کاربری **جلالی text** (مثل `۱۴۰۳-۰۴-۱۵`)؛ تاریخ داخلی `timestamp` میلادی.
- `JWT_SECRET` حتماً ≥۳۲ کاراکتر.
- فرم `/apply` کاملاً داینامیک — فیلد hard-code اضافه نکن.

---

## 📓 ژورنال نشست‌ها (جدیدترین بالا — حداکثر ۷ ورودی)

## 📓 2026-07-25 — merge + deploy فرم «ثبت تراکنش» — تکمیل کامل فاز «Product UI V2» روی main (v0.31.0)
**چه شد:** با تأیید صریح کاربر («merge/deploy کن»)، `feat/product-ui-v2` (آخرین commit: بازطراحی فرم ثبت تراکنش) روی `main` fast-forward شد — بدون conflict، چون `main` از قبل ancestor مستقیم branch بود (دو فاز قبلی همین branch — پایه‌ی UI و داشبورد و لیست تراکنش‌ها — قبلاً در نشست‌های جداگانه merge/deploy شده بودن). Push شد → GitHub Actions run `30168916747` اجرا شد: `typecheck`/`lint`/`unit-test` سبز، `deploy` (Liara) روی attempt اول موفق. با این merge، کل فاز چندروزه‌ی «Product UI V2» (پایه‌ی UI مشترک، بازطراحی IA داشبورد، بازطراحی لیست تراکنش‌ها، بازطراحی فرم ثبت تراکنش) به‌طور کامل روی production قرار گرفت؛ هیچ branch جداگانه‌ای دیگه در کار نیست.
**فایل‌ها:** بدون تغییر کد در این نشست — فقط عملیات git (`checkout main`، `merge --ff-only feat/product-ui-v2`، `push origin main`) + این فایل.
**Build:** CI کامل main (`run 30168916747`): typecheck ✅ · unit-test ✅ · lint ✅ · deploy به Liara ✅ (attempt ۱، بدون خطا).
**ناتمام:** بررسی بصری authenticated واقعی فرم جدید (ثبت یک هزینه/درآمد/انتقال واقعی روی خود production) هنوز با چشم کاربر تأیید نشده.
**برای جلسه‌ی بعد:** از کاربر بخواه یک تراکنش واقعی از `/transactions/new` روی سایت زنده امتحان کنه. اگه مشکلی دید سریع گزارش بده. بعدش: مهاجرت تدریجی بقیه صفحات (غیر از Dashboard/Transactions که الان تکمیل‌ان) به `PageShell`/`FilterToolbar`/`MetricGrid` — هنوز شروع نشده.

## 📓 2026-07-25 — بازطراحی فرم «ثبت تراکنش» — ادامه‌ی `feat/product-ui-v2`
**چه شد:** طبق یک اسپک ۷بخشی دقیق («وضوح، سرعت، پیشگیری از خطای مالی»)، صفحه‌ی `/transactions/new` بدون تغییر منطق حسابداری/تأیید/RBAC/API contract کاملاً بازطراحی شد. اول یک agent تحقیقی، صفحه‌ی فعلی، schema جدول `transactions`، منطق تأیید (نقش‌محور، نه آستانه‌ی مبلغ)، مکانیزم دوطرفه‌ی transfer (یک ردیف، دو account update)، و فرمول VAT رو کامل نقشه‌برداری کرد. **۱) اطلاعات اصلی:** `Toggle` (نوع تراکنش) از `role=group`+دکمه‌های مستقل (بدون کیبورد) به الگوی WAI-ARIA «Radio Group» کامل ارتقا یافت — دقیقاً همون منطق ناوبری (`nextRadioIndex`) که فاز قبل برای `SegFilter` ساخته و تست شده بود، این‌بار با ارتفاع لمسی ۴۴px (نه ۳۲px قبلی) چون این اولین و مهم‌ترین تصمیم فرمه. مبلغ به «قوی‌ترین فیلد» تبدیل شد — فونت ۲۲–۲۶px، ردیف hero مستقل، واحد «تومان» ثابت داخل فیلد؛ فرمت‌کردن ورودی همچنان از `formatAmountInput`/`parseAmount` موجود استفاده می‌کنه (بدون تغییر، از قبل مقدار عددی رو خراب نمی‌کرد). **۲) مسیر مالی:** روش پرداخت و طرف‌حساب (که قبلاً داخل accordion «جزئیات بیشتر» پنهان بودن با اینکه روش پرداخت اجباریه) به بخش همیشه-قابل‌مشاهده «مسیر مالی» منتقل شدن؛ برای انتقال وجه، صندوق مبدا/مقصد کنار هم نمایش داده می‌شن. **۳) تاریخ:** از داخل accordion به یک بخش مستقل و همیشه-دیده منتقل شد (چون فیلد اجباریه — نباید پشت یک کلیک اضافه پنهان باشه). **۴) مالیات/پیش‌فاکتور:** از پنل‌های بزرگ ستونی به ردیف‌های فشرده (چک‌باکس + پیش‌نمایش زنده‌ی مالیات در یک خط) تبدیل شدن؛ فرمول VAT دست‌نخورده موند (فقط UI). **۵) جزئیات بیشتر:** primitive جدید `Disclosure` (`components/ui/Disclosure.tsx`) ساخته شد — الگوی استاندارد WAI-ARIA Disclosure (`aria-expanded`+`aria-controls`+`hidden`، نه رندر شرطی) که تضمین می‌کنه مقدار فیلدهای داخلش (شماره رسید/کد فاکتور/توضیحات — تنها فیلدهای واقعاً اختیاری باقی‌مانده) هنگام جمع‌شدن هرگز گم نشه. **۶) ثبت:** primitive موجود ولی هرگز-استفاده‌نشده‌ی `StickyActionBar` بالاخره برای اولین بار در پروژه استفاده شد؛ خلاصه‌ی خطای قابل‌دسترس (`InlineNotice` role=alert) + فوکوس خودکار به اولین فیلد نامعتبر (`setFocus` از react-hook-form — نیازمند اضافه‌کردن `register('amount')` که قبلاً فقط state دستی داشت، بدون تغییر منطق)؛ محافظت double-submit با یک `ref` سنکرون (علاوه بر disabled شدن دکمه)؛ هشدار خروج بدون ذخیره با `useConfirm()` (نه `window.confirm`) + `beforeunload`؛ مقادیر فرم روی خطای سرور حفظ می‌شن (رفتار از قبل درست بود، حالا تضمین‌شده و تست‌شده). **۷) زمینه‌ی تأیید:** زیرنویس ضعیف «تراکنش شما فوری تایید می‌شود» با یک `InlineNotice` پویا جایگزین شد که بر اساس نقش کاربر و پرچم پیش‌فاکتور، پیامد واقعی (اثر روی موجودی صندوق) رو توضیح می‌ده — بدون تغییر منطق تأیید و بدون افشای جزئیات سیاست داخلی.
**فایل‌ها:** (جدید) `components/ui/Disclosure.tsx`، `tests/e2e/new-transaction-form.spec.ts` (۱۲ تست) · (ویرایش) `app/(app)/transactions/new/page.tsx` (بازنویسی کامل)، `components/ui/Toggle.tsx` (radiogroup+کیبورد)، `components/ui/index.ts` (export `Disclosure`)
**Build:** tsc ✅ ۰ خطا · tests 393/393 ✅ (بدون تغییر — منطق خالص جدیدی اضافه نشد، `Toggle` از تابع تست‌شده‌ی موجود دوباره‌استفاده کرد) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · playwright --list ✅ (۱۲ تست جدید در `new-transaction-form.spec.ts` قابل discover، اجرای واقعی به‌خاطر نبود `.env.e2e` مثل همیشه بلاک) · git diff --check ✅ · اندازه‌ی route: `/transactions/new` قبل ۵.۹۹kB (First Load ۱۸۷kB) → بعد ۶.۹۶kB (First Load ۱۸۹kB) — رشد ~۱kB برای `Disclosure`/`StickyActionBar`/`InlineNotice`/`PageHeader`/`PageShell`/`Checkbox`/`useConfirm`، منطقی
**ناتمام:** merge/deploy عمداً انجام نشد. بررسی بصری authenticated واقعی (پر کردن و ثبت یک تراکنش واقعی با چشم کاربر) این نشست هم دسترسی DB لوکال نداشت — dev server فقط تا ریدایرکت به `/login` بدون خطای کنسول تأیید شد.
**برای جلسه‌ی بعد:** این branch (`feat/product-ui-v2`) رو با کاربر مرور کن؛ ترجیحاً یک تراکنش واقعی (هزینه، درآمد، و انتقال وجه) از فرم جدید در محیط زنده ثبت کن تا مسیر مالی/VAT/پیش‌فاکتور با چشم تأیید بشه — قبل از merge/deploy.

## 📓 2026-07-25 — merge + deploy کامل `feat/product-ui-v2` روی main (v0.31.0)
**چه شد:** با تأیید صریح کاربر («merge و deploy کن»)، branch `feat/product-ui-v2` (که شامل هر سه فاز قبلی بود: پایه‌ی UI مشترک، بازطراحی IA داشبورد، بازطراحی لیست تراکنش‌ها) کامل روی `main` قرار گرفت. **یافته‌ی مهم قبل از merge:** بخش ۰ی این فایل خودش stale بود — نوشته بود «main تا `cc87968` دیپلوی شده، branch عمداً merge/deploy نشده»، ولی `git fetch` نشون داد `origin/main` از قبل روی `28221b3` (بازطراحی داشبورد) بود؛ یعنی merge دو فاز اول (پایه‌ی UI `9b4ad6c` + داشبورد `28221b3`) توی یک نشست قبلی (که این نشست ادامه‌ش بود) واقعاً اتفاق افتاده بود، فقط ژورنال بعدش به‌روز نشده بود. با چک مستقیم GitHub Actions API (`run 30147639721`) تأیید شد اون deploy هم روی attempt دوم موفق شده بود — یعنی مشکل قبلی `LIARA_API_TOKEN` منقضی که این merge رو یک‌بار بلاک کرده بود، توسط کاربر (بین نشست‌ها) حل شده بود. با این تأیید، merge سومین و آخرین commit (`67397a1`، بازطراحی تراکنش‌ها) روی `main` یک **fast-forward تمیز** بود (بدون conflict، چون `main` از قبل ancestor مستقیم branch بود). Push شد → GitHub Actions run `30158926517` اجرا شد: `typecheck`/`lint`/`unit-test` سبز، `e2e` skip (طبق معمول، `.env.e2e` غایب)، `deploy` (Liara) روی **attempt اول موفق** شد — یعنی توکن Liara الان معتبره و پایداره.
**فایل‌ها:** بدون تغییر کد در این نشست — فقط عملیات git (`checkout main`، `merge --ff-only feat/product-ui-v2`، `push origin main`) + این فایل.
**Build:** CI کامل main (`run 30158926517`): typecheck ✅ · unit-test ✅ · lint ✅ · e2e skipped (بدون تغییر) · deploy به Liara ✅ (attempt ۱، بدون خطا).
**ناتمام:** بررسی بصری authenticated واقعی (لاگین SuperAdmin روی خود production، دیدن داشبورد/تراکنش‌های جدید با چشم) هنوز انجام نشده — این‌بار چون همه‌چیز مستقیم روی production رفت، ارزش داره کاربر یک نگاه سریع به `/dashboard` و `/transactions` روی سایت واقعی بندازه تا قبل گزارش «تمام» تلقی بشه.
**برای جلسه‌ی بعد:** ۱) از کاربر بخواه یک نگاه به نسخه‌ی زنده‌ی `/dashboard` و `/transactions` بندازه (خصوصاً Popover فیلتر پیشرفته‌ی تراکنش‌ها، Sheet موبایل، و بخش «تراز مالی» جدید داشبورد) — اگه مشکلی دید سریع گزارش بده. ۲) طبق «کار بعدی پیشنهادی» قبلی: مهاجرت تدریجی بقیه صفحات (غیر از Dashboard/Transactions که الان تکمیل‌ان) به `PageShell`/`FilterToolbar`/`MetricGrid` — هنوز شروع نشده، اگه کاربر خواست ادامه بده.

## 📓 2026-07-25 — بازطراحی لیست تراکنش‌ها — ادامه‌ی `feat/product-ui-v2`
**چه شد:** طبق یک اسپک ۷بخشی دقیق، صفحه‌ی `/transactions` بدون تغییر منطق حسابداری/تأیید/RBAC کاملاً بازطراحی شد. اول یک agent تحقیقی، API دقیق پرایمیتیوهای موجود (`PageHeader`، `MetricGrid`، `Sheet`، `InlineNotice`، `IconButton`، `Chip`، `StatusPill`، `MetricCard`، `Skeleton`) و state شعبه‌ی `TransactionsSlice` (`txError`/`txLoading`) رو نقشه‌برداری کرد؛ یافته‌ی مهم: `txError` قبلاً هیچ‌جا در UI نمایش داده نمی‌شد (خطای عملیات تأیید/رد/ویرایش بی‌صدا گم می‌شد) — این یک باگ واقعی a11y/UX بود که این فاز رفعش کرد. **۱) هدر:** `PageToolbar` با دکمه‌ی اصلی «ثبت تراکنش» + یک `Popover` disclosure («⋮ بیشتر») که چاپ و `ImportPanel` رو نگه می‌داره (بدون `role=menu` چون شامل فرم تعاملیه، نه لیست اکشن ساده). **۲) خلاصه‌ها:** ماژول خالص جدید `lib/transactions/filterTransactions.ts` — `summarizeTransactions()` روی نتیجه‌ی فیلترشده حساب می‌کنه، فیلد صراحتاً `periodNetFlow` نام‌گذاری شده (نه `balance`/موجودی، طبق دستور اسپک)؛ متریک «در انتظار تأیید» قابل‌کلیکه (فیلتر status=pending رو اعمال می‌کنه). خط جدید «دوره: … · شعبه: …» دامنه‌ی فعلی رو صریح نشون می‌ده. **۳) فیلترها:** state واحد `TransactionFilterState` با URL two-way sync (`paramsToFilters`/`filtersToParams`، `router.replace`)؛ جستجو با debounce ۳۰۰ms (state ورودی فوری جدا از state فیلترشده، برای جلوگیری از محاسبه‌ی مجدد روی هر ضربه)؛ فیلترهای پرتکرار (نوع/وضعیت/شعبه) همیشه دیده می‌شن، بازه‌ی تاریخ+مرتب‌سازی داخل یک `Popover` پیشرفته با badge تعداد؛ چیپ‌های فیلتر فعال قابل حذف تکی؛ صفحه‌بندی client-side جدید (`paginateItems`، ۲۰تایی) با state در URL؛ Sheet موبایل جدا با همه‌ی فیلترها یک‌جا. **۴) جدول دسکتاپ:** `DataList` با prop جدید `stickyHeader`/`maxHeight` (قبلاً فقط در type تعریف شده بود، پیاده‌سازی واقعی نداشت — تکمیل شد: `sticky top-0` روی `<thead>` + `overflow-y-auto` روی `TableContainer` با `maxHeight` — نیازمند اضافه‌کردن `style` prop به `TableContainer` که قبلاً نبود)؛ شناسه‌ی فاکتور حالا `dir=ltr` isolate داره (قبلاً نداشت). **۵) موبایل:** باگ واقعی رفع شد — ستون‌های تاریخ و شعبه قبلاً `mobileHide:true` داشتن (مستقیماً نقض اسپک «تاریخ و شعبه باید در کارت موبایل دیده بشن»)؛ حذف شد. **۶) حالت‌های خالی:** تفکیک صریح «هنوز تراکنشی ثبت نشده» (`visible.length===0`) از «نتیجه‌ای برای این فیلترها نیست» (فیلترشده خالی ولی داده هست) با CTA متفاوت برای هرکدوم؛ `txError` حالا با `InlineNotice` قابل-dismiss همیشه بالای صفحه دیده می‌شه (نه پشت empty state). **۷) لوکالیزیشن:** primitive جدید `Popover` (`components/ui/Popover.tsx`) با دو حالت — disclosure ساده و `role=menu` با ناوبری Arrow/Home/End؛ تابع جدید `formatCategoryDisplay()` در `format.ts` (نگاشت نمایشی خالی فعلاً، زیرساخت آماده، هرگز داده‌ی خام رو تغییر نمی‌ده).
**فایل‌ها:** (جدید) `lib/transactions/filterTransactions.ts`، `components/ui/Popover.tsx`، `tests/unit/filterTransactions.test.ts` · (ویرایش) `app/(app)/transactions/page.tsx` (بازنویسی کامل)، `components/ui/DataList.tsx` (تکمیل `stickyHeader`)، `components/ui/Table.tsx` (`style` prop روی `TableContainer`)، `components/ui/index.ts` (export `Popover`)، `lib/design/format.ts` (`formatCategoryDisplay`)، `tests/unit/format.test.ts`، `tests/e2e/transactions.spec.ts` (۵ تست جدید: پایداری فیلتر URL، کیبورد، دو حالت خالی، رندر موبایل)، `HANDOFF.md`
**Build:** tsc ✅ ۰ خطا · tests 393/393 ✅ (۲۴ جدید در `filterTransactions.test.ts` + ۲ در `format.test.ts`) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · playwright --list ✅ (۵ تست جدید در `transactions.spec.ts`، اجرای واقعی به‌خاطر نبود `.env.e2e` مثل همیشه بلاک) · git diff --check ✅ · اندازه‌ی route: قبل `/transactions` 11.9kB (First Load 196kB) → بعد 14.9kB (First Load 199kB) — رشد ~3kB برای فیلتر/صفحه‌بندی/Popover/Sheet جدید، منطقی
**ناتمام:** merge/deploy عمداً انجام نشد. بررسی بصری authenticated واقعی (لاگین SuperAdmin، دیدن جدول/کارت‌ها با داده‌ی واقعی) این نشست هم دسترسی DB لوکال نداشت — dev server بدون DB فقط تا redirect به `/login` تست شد.
**برای جلسه‌ی بعد:** این branch (`feat/product-ui-v2`) رو با کاربر مرور کن؛ ترجیحاً با یک نگاه بصری واقعی (لاگین با حساب SuperAdmin) به `/transactions` جدید در ۳۹۰/۷۶۸/۱۲۸۰/۱۹۲۰px — به‌خصوص Popover فیلتر پیشرفته، Sheet موبایل، و کارت موبایل با تاریخ/شعبه‌ی حالا-قابل‌مشاهده — قبل از merge. اگه تأیید شد، تصمیم بگیر همراه بازطراحی داشبورد یک‌جا merge/deploy بشه یا جدا.

## 📓 2026-07-24 — بازطراحی کامل معماری اطلاعات داشبورد — ادامه‌ی `feat/product-ui-v2`
**چه شد:** طبق یک اسپک دقیق، Information Architecture داشبورد کاملاً بازطراحی شد — بدون تغییر منطق تأیید/RBAC/schema. اول یک agent تحقیق کامل صفحه/APIهای موجود/تمام کامپوننت‌های `components/dashboard` رو نقشه‌برداری کرد؛ یافته‌ی کلیدی: `useDashboardMetrics` هیچ مفهوم بازه‌ی زمانی نداشت (کل تاریخچه‌ی تراکنش‌ها را client-side جمع می‌زد) و فیلد `balance`ش واقعاً یک FLOW دوره‌ای بود، نه موجودی حساب — دقیقاً برچسب گمراه‌کننده‌ای که اسپک خواسته بود اصلاح شود. **۱) هدر:** `DashboardScopeBar` جدید — انتخاب بازه (امروز/۷روز/۳۰روز/دلخواه با `SegFilter`)، آخرین به‌روزرسانی، دکمه‌ی رفرش با loading، همگام با URL (`?branch=&period=&from=&to=`). **۲) نیازمند توجه:** `AttentionWidget` بازنویسی شد — هر آیتم حالا گروه (مالی/انبار/عملیات/پرسنل) + شدت با متن (نه فقط رنگ پس‌زمینه‌ی آیکن، برای «no رنگ‌به‌تنهایی») دارد؛ یک آیتم «پرسنل» جدید (دوره‌ی حقوق منتظر بررسی) اضافه شد؛ حالت خالی همچنان نوار باریک است. **۳) وضعیت امروز:** `FlashReportCard` یک باگ واقعی داشت — هیچ‌وقت `branchId` را در fetch پاس نمی‌داد (همیشه همه‌ی شعب، حتی با فیلتر شعبه‌ی هدر) — رفع شد؛ «میانگین هر فاکتور» اضافه شد (null صریح نه صفر وقتی فاکتوری نیست)؛ برچسب‌های انگلیسی COGS/Prime Cost به فارسی («بهای تمام‌شده»، «نسبت هزینه مستقیم») با اصطلاح فنی در tooltip؛ حالت «امروز هیچ فعالیتی نبوده» به‌جای گرید صفرهای بی‌معنی. **۴) تراز مالی:** بزرگ‌ترین اصلاح معنایی — عنوان از «موجودی (تراکنش‌ها)» به «جریان خالص دوره» تغییر کرد؛ موجودی واقعی حساب‌ها (STOCK) در یک بخش کاملاً جدا و صریحاً برچسب‌گذاری‌شده («لحظه‌ای، مستقل از بازه») نمایش داده می‌شود؛ هشدار تمام‌عرض هزینه‌ی راه‌اندازی به یک Chip فشرده تبدیل شد؛ محاسبه از `/api/reports` (endpoint موجود گزارش مالی) با بازه‌ی هدر — نه محاسبه‌ی جدید تکراری. **۵) روند:** انتخاب ۱۴/۳۰/۹۰ روز، نوار مقایسه با دوره‌ی قبل، جدول مخفی (`sr-only`) برای screen reader، ارتفاع ثابت در همه‌ی حالت‌ها (loading/کم‌داده/نمودار) — قبلاً با `return null` کل کارت جابه‌جا می‌شد. **۶) مقایسه‌ی شعب:** endpoint جدید `/api/dashboard/branch-comparison` (فقط SuperAdmin) برای COGS/ضایعات به‌تفکیک شعبه — با `byBranch` موجودِ `/api/reports` بر اساس branchId ادغام می‌شود، نه بازنویسی منطق income/expense. **۷) صف‌های عملیاتی:** بخش کاملاً جدید — تمام صف‌ها (تأیید معلق، برگه‌ی انبار، کمبود موجودی، PO باز، تعمیرات، وظایف) همیشه نمایش داده می‌شوند (حتی صفر = خبر خوب)، هرکدام لینک به مقصد فیلترشده (`?status=pending` به transactions هم اضافه شد چون قبلاً از URL نمی‌خوند). **۸) پرسنل/شرکا:** `HRSummaryCard` از `div onClick` (فقط ماوس) به `button` واقعی تبدیل شد؛ داوطلبان تازه حالا چیپ «جدید — منتظر بررسی» دارند؛ آورده‌ی شریکِ تنظیم‌نشده به‌جای «۰ تومان» یک اقدام («تنظیم آورده ←») نشون می‌ده. **رفع یک باگ RBAC واقعی:** `/api/dashboard/trends` هیچ enforcement شعبه‌ای نداشت — فقط `requireSession()` صدا می‌زد بدون چک `session.branchId`؛ یک BranchUser با فرستادن `?branchId` دلخواه می‌تونست روند مالی شعبه‌ی دیگه رو ببینه — رفع شد با همون الگوی SuperAdmin-آزاد/غیرمدیر-قفل که در فازهای قبل جا افتاده. **حذف ۳ درخواست تکراری:** `AttentionWidget`+`HRSummaryCard`+`OperationalQueues` هرکدوم مستقل `/api/dashboard/overview` را fetch می‌کردند؛ حالا صفحه یک‌بار fetch می‌کند و prop می‌ده (الگوی مشابه برای `/api/reports`). `TodayCashFlow` (که با `TrendChart` یک endpoint رو تکراری می‌زد) و `AnomalyBanner` (که با `AttentionWidget` یک endpoint رو تکراری می‌زد) کاملاً حذف شدند. `useDashboardMetrics.ts` (محاسبه‌ی client-side قدیمی) هم حذف شد — جای دیگه‌ای استفاده نمی‌شد. **باگ build واقعی پیدا و رفع شد:** import کردن یک تابع خالص از فایلی که `db client` (پستگرس) هم import می‌کرد باعث شد کل کد اتصال DB وارد باندل مرورگر بشه و build با «Module not found: perf_hooks» بشکنه — فایل به دو تا تقسیم شد: `periodResolve.ts` (خالص، بدون DB، برای client) و `periodReport.ts` (فقط سرور).
**فایل‌ها:** (جدید) `lib/reports/periodResolve.ts`، `lib/reports/periodReport.ts`، `lib/reports/metricSemantics.ts`، `app/api/dashboard/branch-comparison/route.ts`، `components/dashboard/DashboardScopeBar.tsx`، `components/dashboard/FinancialPosition.tsx`، `components/dashboard/OperationalQueues.tsx`، `components/dashboard/overviewTypes.ts`، `tests/unit/period-report.test.ts`، `tests/unit/dashboard-metric-semantics.test.ts`، `tests/unit/dashboard-trends-route.test.ts`، `tests/unit/dashboard-branch-comparison-route.test.ts`، `tests/e2e/dashboard-redesign.spec.ts` · (ویرایش) `app/(app)/dashboard/page.tsx` (بازنویسی کامل)، `app/(app)/transactions/page.tsx` (خواندن `status` از URL)، `app/api/dashboard/trends/route.ts` (رفع RBAC + پارامتر days + previousTotal)، `components/dashboard/AttentionWidget.tsx`، `BranchSummary.tsx`، `FlashReportCard.tsx`، `HRSummaryCard.tsx`، `RecruitmentWidget.tsx`، `TrendChart.tsx`، `index.ts` · (حذف) `components/dashboard/AnomalyBanner.tsx`، `components/dashboard/TodayCashFlow.tsx`، `lib/hooks/useDashboardMetrics.ts`
**Build:** tsc ✅ ۰ خطا · tests 367/367 ✅ (۴۰ جدید) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · playwright --list ✅ ۷۱ تست (۸ جدید) · git diff --check ✅ · بررسی بصری با Chrome: dev server بدون خطای کنسول بالا اومد، `/dashboard` درست به `/login` ریدایرکت شد (بدون DB لوکال، لاگین واقعی ممکن نبود)
**ناتمام:** merge/deploy عمداً انجام نشد. بررسی بصری authenticated واقعی داشبورد جدید هنوز با چشم کاربر تأیید نشده (این نشست دسترسی DB لوکال نداشت). Playwright واقعی هنوز به‌خاطر `.env.e2e` بلاک است.
**برای جلسه‌ی بعد:** این branch رو با کاربر مرور کن؛ ترجیحاً با یک نگاه بصری واقعی (لاگین با حساب SuperAdmin) به داشبورد جدید در ۳۹۰/۱۴۴۰/۱۹۲۰px قبل از merge.

## 📓 2026-07-24 — «Product UI V2»: پایه‌ی UI مشترک — روی branch جدا `feat/product-ui-v2`
**چه شد:** طبق یک درخواست گسترده برای یکدست‌سازی کامل UI، اول یک ممیزی کامل (agent جدا) نشون داد سیستم طراحی همچنان بالغه (۳۳ کامپوننت مشترک، توکن‌های معنایی کامل) — پس مثل فازهای قبلی، به‌جای بازسازی، شکاف‌های واقعی بسته شدن + ۷ primitive جدیدِ واقعاً غایب ساخته شدن: **۱) کنترل‌ها:** `Input`/`Select`/`Textarea` رینگ فوکوس‌شون `ring-accent/20` بود درحالی‌که `Button`/`IconButton`/`Checkbox`/`Switch` از `ring-accent/40` استفاده می‌کردن — یکدست به `/40` شد؛ ارتفاع `Input`/`Select` به `h-11 sm:h-10` تغییر کرد (۴۴px لمسی موبایل، ۴۰px فشرده دسکتاپ). `SegFilter` کاملاً بازنویسی شد: قبلاً هیچ focus ring نداشت و فقط با ماوس کار می‌کرد؛ حالا الگوی WAI-ARIA «Radio Group» کامل — `role=radiogroup`+`radio`، roving tabIndex، ناوبری ArrowLeft/Right/Home/End (با معکوس‌سازی صحیح در RTL). `TableContainer` هیچ `overflow-x-auto` نداشت (فقط `overflow-hidden` — یعنی روی موبایل جدول‌های عریض کلیپ می‌شدن نه اسکرول)؛ رفع شد. `DataList`/`Th` سطرهای کلیک‌پذیر و ستون‌های مرتب‌سازی‌شونده فقط با ماوس کار می‌کردن (بدون role/tabIndex/کیبورد) — رفع شد. `Button`/`IconButton` حالا `aria-busy` دارن در حالت loading. **۲) ۷ primitive جدید ساخته شد** (هیچ‌کدوم قبلاً وجود نداشت، با grep تأیید شد): `PageShell` (کانتینر صفحه با ۳ نوع عرض: form/detail/data — رفع فضای خالی در ۱۴۴۰-۱۹۲۰px برای صفحات داده‌محور)، `PageToolbar` (هدر صفحات سطح‌بالا بدون دکمه‌ی بازگشت، برخلاف `PageHeader` موجود)، `FilterToolbar`، `MetricGrid` (با CSS `auto-fit`/`minmax` به‌جای breakpoint ثابت — دقیقاً رفع مشکل «گرید ثابت که نصف صفحه رو خالی می‌ذاره»)، `FormSection`، `StickyActionBar`، `InlineNotice` (بنر پیام درون‌صفحه‌ای با نگاشت tone→role/aria-live دقیقاً هم‌قرارداد با Toast: warning/danger=alert/assertive، info/success=status/polite؛ هرگز فقط با رنگ، همیشه با آیکن مجزا). `StatusBadge` به‌عنوان نام مستعار `StatusPill` (بدون duplicate) اضافه شد. **۳) شکاف واقعی در Header:** یک `<div>` کامل خالی با کامنت «GlobalBranchSelector بعداً اینجا mount می‌شه» که هیچ‌وقت اتفاق نیفتاد — نصف راست هدر همیشه خالی بود. `BranchContextBadge` جدید ساخته و mount شد (فقط نمایشی، نه سوییچ‌کننده — بدون تغییر منطق دسترسی). **۴) فرمت مبلغ:** یک نمونه‌ی دیگه از باگ «علامت دستی جدا از isolate» که فاز قبل پیدا شده بود، در `ContactLedgerDrawer.tsx` هم بود (خارج از scope فاز قبل) — با `formatSignedMoney` یکسان‌سازی شد. **۵) a11y:** `Sheet` (bottom sheet موبایل) فوکوس مدیریت نمی‌کرد — نه هنگام باز شدن، نه focus trap، نه برگشت فوکوس هنگام بسته شدن (برخلاف `ConfirmDialog` که این‌ها رو درست داشت) — رفع شد با همون الگوی `ConfirmDialog`. **۶) مهاجرت سبک** (طبق دستور صریح، بدون redesign عمیق): Dashboard/Transactions/Recruitment به `PageShell`/`PageToolbar`/`MetricGrid` مهاجرت کردن — فقط ساختار container، بدون تغییر منطق کسب‌وکار؛ حین این کار یک `ref` مرده (`printRef` در transactions، تعریف‌شده ولی هرگز استفاده‌نشده) پاک شد. بقیه‌ی صفحات (که Quick Win‌های ممیزی بصری قبلی — overflow-x-auto جداول، گرید موبایل — رو از قبل داشتن، تأیید شد با بررسی مستقیم) عمداً دست‌نخورده موندن.
**فایل‌ها:** (ویرایش) `components/ui/Input.tsx`، `Select.tsx`، `Textarea.tsx`، `SegFilter.tsx`، `Table.tsx`، `DataList.tsx`، `Button.tsx`، `IconButton.tsx`، `Card.tsx`، `PageHeader.tsx`، `index.ts`، `components/layout/Header.tsx`، `components/contacts/ContactLedgerDrawer.tsx`، `app/(app)/dashboard/page.tsx`، `transactions/page.tsx`، `recruitment/page.tsx`، `tasks/page.tsx` · (جدید) `components/ui/PageShell.tsx`، `PageToolbar.tsx`، `FilterToolbar.tsx`، `MetricGrid.tsx`، `FormSection.tsx`، `StickyActionBar.tsx`، `InlineNotice.tsx`، `components/layout/BranchContextBadge.tsx`، `tests/unit/product-ui-v2-primitives.test.ts`، `tests/e2e/product-ui-v2.spec.ts`، `.claude/launch.json`
**Build:** tsc ✅ ۰ خطا · tests 327/327 ✅ (۱۴ جدید) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · playwright --list ✅ ۶۳ تست (۱۴ جدید) · git diff --check ✅ · بررسی بصری دستی با Chrome روی `/login` در ۳۷۵px و دسکتاپ (بدون DB لوکال، امکان لاگین واقعی نبود؛ فقط تأیید بصری صفحه‌ی عمومی)
**ناتمام:** merge/deploy عمداً انجام نشد. مهاجرت `PageShell`/`FilterToolbar`/`MetricGrid` فقط روی ۳ صفحه‌ی نام‌برده‌شده انجام شد؛ بقیه‌ی ~۳۰ صفحه هنوز کانتینر/هدر دستی خودشون رو دارن (عمدی — بلاست ردیوس این فاز محدود به primitive+۳ صفحه بود). مقیاس تایپوگرافی جدید تعریف شد ولی روی نمونه‌های قدیمی arbitrary (`text-[11.5px]` و مشابه، >1400 مورد) retrofit نشد (عمدی، ریسک بازرگشتی بالا برای سود صرفاً بصری).
**برای جلسه‌ی بعد:** این branch (`feat/product-ui-v2`) رو با کاربر مرور کن؛ اگه تأیید شد push کن. لیست ادامه‌ی طبیعی: مهاجرت تدریجی بقیه صفحات به primitive‌های جدید، صفحه‌به‌صفحه.

## 📓 2026-07-23 — تکمیل و release «UI/UX Refresh Phase 1» + یکدست‌سازی کامل کنترل‌ها (v0.30.0)
**چه شد:** ادامه‌ی مستقیم جلسه‌ی قبل — این‌بار merge/deploy صراحتاً مجاز بود. ۱) **ممیزی کامل کنترل‌ها**: تمام ۲۹۸ `<button>` خام در ۸۱ فایل شمارش و دسته‌بندی شد (نه جایگزینی کورکورانه). ۲) **کامپوننت مشترک جدید `IconButton`** (`components/ui/IconButton.tsx`) — جایگزین ۲۶ نمونه‌ی تقریباً یکسان از الگوی `w-{7,8,9} h-{7,8,9} flex items-center justify-center rounded` (دکمه‌های بستن/رفرش/ویرایش/حذف/خروج) در `NotificationsBell`، `Sidebar` (×۲)، `AdminSidebar` (×۲)، `BottomTabBar`، `RecipientDrawer`، صفحه‌ی قوانین اعلان ادمین (×۲)، و ردیف‌اکشن‌های `accounts`/`contacts`/`notifications` (×۱۱ جمعاً) — با `aria-label` اجباری (چند مورد فقط `title` داشتن، خلأ دسترس‌پذیری واقعی رفع شد)، تم روشن/تیره، spinner داخلی. ۳) **`ButtonLink` جدید** برای ناوبری با ظاهر دکمه. ۴) **سایز جدید `Button` به نام `field` (h-10)** — دقیقاً هم‌ارتفاع با `Input`/`Select` (که قبلاً با پیش‌فرض `md` دکمه ۴px بلندتر بودن). ۵) **یافته‌ی عملکردی واقعی**: اضافه‌کردن یک `IconButton` به `notifications/page.tsx` (که قبلاً هیچ‌وقت از barrel مشترک `@/components/ui` import نکرده بود) حجمش رو از ۱۲۱KB به ۱۶۱KB برد (+۴۰KB!) — چون این barrel به‌طور کامل tree-shake نمی‌شه. با تغییر import به مسیر مستقیم فایل (`@/components/ui/IconButton` به‌جای barrel) در همه‌ی import‌های جدید این sweep (شامل هر دو layout ریشه که قبلاً هرگز از barrel استفاده نکرده بودن) حل شد؛ نتیجه: notifications به ۱۲۲KB برگشت. ۶) استثناهای عمدی مستندشده: `Switch`/`SegFilter`/`Toggle`/دکمه‌های حذف چیپ/تب‌ها — معنای متفاوت دارن، تبدیل نشدن. ۷) ~۱۸ نمونه‌ی مشابه باقیمانده در coupons/reservations/partners/purchase-orders برای Phase ۲ مستند شد (خارج از صفحات درخواست‌شده‌ی صریح). ۸) merge fast-forward به main → push → دیپلوی (release v196) بدون خطا؛ smoke test فقط از لاگ سرور تأیید شد (دسترسی مرورگر این نشست قطع بود).
**فایل‌ها:** `components/ui/IconButton.tsx` (جدید)، `components/ui/ButtonLink.tsx` (جدید)، `components/ui/Button.tsx`، `components/ui/index.ts`، هر دو layout ریشه، `NotificationsBell.tsx`، `Sidebar.tsx`، `AdminSidebar.tsx`، `BottomTabBar.tsx`، `RecipientDrawer.tsx`، صفحه‌ی قوانین اعلان ادمین، `accounts/page.tsx`، `contacts/page.tsx`، `notifications/page.tsx`، `tests/e2e/ui-ux-refresh-phase-1.spec.ts`، `project-docs/UI-UX-REFRESH-PHASE-1.md`
**Build:** tsc ✅ ۰ خطا · tests 280/280 ✅ · build ✅ · lint ✅ · playwright --list ✅ ۴۰ تست (۱۱ جدید/به‌روزشده) · git diff --check ✅
**ناتمام:** بررسی بصری با چشم کاربر هنوز انجام نشده (فقط لاگ سرور).
**برای جلسه‌ی بعد:** لیست Phase ۲ داخل `UI-UX-REFRESH-PHASE-1.md` رو نگاه کن — اولویت اول: مهاجرت ۱۸ دکمه‌ی آیکونی باقیمانده در ۴ صفحه‌ی دیگه.

<!-- archived to project-docs/handoff-archive.md: 2026-07-14 payroll approve + تأیید نهایی فاز ۱ v0.24.0 + 2026-07-14 فاز امنیتی ۱ + 2026-07-15 استقرار جریان تک‌نشست + 2026-07-15 تست رگرسیون P&L + 2026-07-15 جداسازی E2E + 2026-07-15 notification فاز ۱ + 2026-07-16 Notification Center V2 اولیه + 2026-07-16 اصلاح ۱۵‌گانه NC V2 + 2026-07-16 ممیزی مستقل ۹‌بخشی NC V2 + 2026-07-16 تست‌های رفتاری NC V2 + 2026-07-16 تکمیل کامل NC V2 (۱۱ بخش) + 2026-07-17 اصلاح release-blocker + 2026-07-18 NC V2 در production + scheduler داخل‌پروسه + 2026-07-18 پنل Super Admin موبایل‌فرندلی + 2026-07-18 کنترل گیرندگان اعلان + 2026-07-19 release کنترل گیرندگان اعلان + 2026-07-19 رفع باگ بحرانی crash کامل سایت + دانلود دسته‌ای رزومه + 2026-07-23 ESLint در CI + شروع UI/UX Refresh Phase 1 + 2026-07-23 اصلاح متمرکز باگ‌های موبایل و RTL -->

