# HANDOFF.md — دفتر وضعیت زنده‌ی پروژه «با شرف»

> قانون طلایی: **هر جلسه اول بخش ۰ را بخوان؛ آخر جلسه بخش ۰ + ژورنال را به‌روز کن و commit/push کن.**
> جزئیات تاریخی قدیمی‌تر: `project-docs/handoff-archive.md`.

---

## ⚡ بخش ۰ — وضعیت لحظه‌ای (اول این را بخوان)

| | |
|---|---|
| **نسخه** | `feat/product-ui-v2` (کامیت `3a86fbd`، جلوتر از `main`) — ممیزی نهایی «Product UI V2» روی Dashboard/Transactions/New Transaction/Recruitment تکمیل و push شد. **merge به main / deploy عمداً انجام نشده** — طبق دستور صریح تسک. |
| **آخرین به‌روزرسانی** | 2026-07-26 |
| **Build/tsc** | روی `feat/product-ui-v2` (`3a86fbd`): tsc ✅ ۰ خطا · tests 420/420 ✅ · build ✅ · lint ✅ · playwright --list ✅ ۱۰۳ تست · git diff --check ✅ |
| **دیپلوی** | main هنوز روی `58544d6` (بدون تغییر از قبل). این branch دیپلوی نشده. |
| **کار نیمه‌تمام (in-progress)** | — |
| **کار بعدی پیشنهادی** | از کاربر بخواه تأیید کنه که `feat/product-ui-v2` رو merge/deploy کنم؛ چک‌لیست release در پیام پایانی نشست هست. بعدش: مهاجرت تدریجی بقیه صفحات به `PageShell`/`FilterToolbar`/`MetricGrid` (کار قدیمی معلق). |
| **بلاک‌شده/منتظر کاربر** | ۱) Playwright e2e ⛔ — `.env.e2e` غایب (بدون تغییر، چند ماهه). ۲) منشأ `below_approval_limit.email_enabled=true` (از قبل production) هنوز چک نشده. ۳) Phase 2 قدیمی (`UI-UX-REFRESH-PHASE-1.md`): ۱۸ دکمه‌ی آیکونی باقیمانده، مهاجرت رنگ dark پنل ادمین — هنوز روی میز. ۴) `loadBranchCogsWaste` (Dashboard) هنوز fetch خطا رو بی‌صدا catch می‌کنه — تأثیر محدود (فقط ستون COGS/waste جدول مقایسه‌ی شعب، فقط SuperAdmin)، عمداً از این ممیزی خارج نگه داشته شد. |

> ⚠️ **نکته مهم برای جلسات بعدی:** فرم `/apply` حالا کاملاً داینامیک و دیتابیس‌محور است. **دیگر فیلد hard-code به `app/apply/page.tsx` یا `lib/recruitment/` اضافه نکنید.** همه فیلدهای جدید باید از طریق `/recruitment/form-builder` ایجاد شوند.

---

## 📋 پروتکل جلسه

### شروع هر جلسه (الزامی — به ترتیب)
⛔ **تنها یک نشست فعال Claude Code مجاز است روی این پوشه ویرایش کند — دو نشست همزمان باز نکن.**
1. این فایل، بخش ۰ + جدیدترین ورودی ژورنال را بخوان.
2. `git status` بزن — اگر تغییرات commit‌نشده هست، یعنی جلسه‌ی قبل ناقص بسته شده: اول وضعیت را با کاربر روشن کن، چیزی را کورکورانه commit یا revert نکن.
3. `git log -5 --oneline` — تطبیق بده با ژورنال.
4. به کاربر خلاصه بگو: «وضعیت X است، کار نیمه‌تمام Y، پیشنهاد بعدی Z» و منتظر تأیید بمان.

### پایان هر جلسه / بعد از هر تغییر معنادار (الزامی)
1. یک ورودی ژورنال با **قالب زیر** به بالای بخش ژورنال اضافه کن.
2. بخش ۰ را به‌روز کن (نسخه، تاریخ، کار نیمه‌تمام، کار بعدی).
3. اگر ژورنال بیش از **۷ ورودی** شد، قدیمی‌ها را به `project-docs/handoff-archive.md` منتقل کن.
4. `git add -A && git commit -m "..." && git push` — **بدون push جلسه را نبند.**
5. ⛔ **دیگر ZIP نساز** — GitHub Actions خودکار deploy می‌کند (از 2026-06-24).

### قالب اجباری ورودی ژورنال
```markdown
## 📓 [تاریخ] — [عنوان کوتاه]
**چه شد:** (۲–۵ خط، تصمیم‌های مهم + چرایی)
**فایل‌ها:** (مسیر کامل ایجاد/ویرایش‌شده‌ها)
**Build:** tsc/build سبز یا خطا + متن خطا
**ناتمام:** دقیقاً کجا متوقف شد، چه چیزی نیمه‌کاره است (اگر هیچ: «—»)
**برای جلسه‌ی بعد:** کار بعدی مشخص + هر هشداری که جلسه‌ی بعد باید بداند
```

---

## 🏗 بخش ۴ — مرجع پایدار (قبل از دست‌زدن به مالی بخوان)

- `accounts.balance` فیلد **cache** است: همیشه از reverse transaction‌ها محاسبه شده. هر تراکنش معکوس جا مانده = drift دائمی.
- ⚠ **INVARIANT فاز ۵**: `accounts.balance` هرگز توسط فیلتر `is_setup` (لنز عملیاتی) تغییر نمی‌کند. این فیلتر فقط روی KPI‌های جریان مالی (income/expense/profit) در داشبورد و گزارش‌ها اثر دارد — ترازنامه مستقل است.
- پول **bigint تومان** در DB؛ در TypeScript `number` (JS safe integer تا ۹۰۰۰ تریلیون کافی است).
- تاریخ کاربری **جلالی text** (مثل `۱۴۰۳-۰۴-۱۵`)؛ تاریخ داخلی `timestamp` میلادی.
- `JWT_SECRET` حتماً ≥۳۲ کاراکتر.
- فرم `/apply` کاملاً داینامیک — فیلد hard-code اضافه نکن.

---

## 📓 ژورنال نشست‌ها (جدیدترین بالا — حداکثر ۷ ورودی)

## 📓 2026-07-26 — ممیزی نهایی «Product UI V2» روی ۴ صفحه — push شد، merge/deploy عمداً نه
**چه شد:** طبق دستور صریح («ممیزی نهایی Product UI V2 روی feat/product-ui-v2؛ داشبورد+تراکنش‌ها+ثبت‌تراکنش+استخدام رو با هم ممیزی کن؛ فقط رگرسیون/ناسازگاری تأییدشده رو رفع کن، فیچر جدید نه»)، یک agent تحقیقی ابتدا اینونتوری کامل (PageHeader/Toolbar، دکمه‌ها، ورودی‌ها، کارت‌ها، فرمت پول، حالت‌های خالی/خطا) رو روی هر ۴ صفحه انجام داد؛ هر یافته قبل از رفع، شخصاً روی کد واقعی چک شد (نه اعتماد به خلاصه‌ی agent) — ۴ مورد false-positive (aria-label اضافی روی دکمه‌های متن‌دار، دکمه‌ی از‌قبل‌درست فیلتر متریک تراکنش‌ها، radius عمدی مودال‌ها، fetch جداگانه‌ی anomaly که تکراری نبود) کنار گذاشته شد، ~۹ مورد واقعی رفع شد. **مهم‌ترین یافته:** سه fetch داشبورد (`loadReports`، `loadOverview`، `loadBranchCogsWaste`) خطا رو کاملاً بی‌صدا catch می‌کردن؛ `FinancialPosition`/`OperationalQueues` با `data=null` کلاً `return null` می‌دادن — یعنی قطعی شبکه دقیقاً شبیه «همه‌چیز مرتب است» به نظر می‌رسید (خصوصاً خطرناک برای `AttentionWidget` که آیتم‌هاش با optional chaining رو `overview` ساخته می‌شن: فچ ناموفق = «هیچی نیاز به توجه نداره» غلط). رفع شد: `reportsError`/`overviewError` state جدا از abort عمدی (تغییر فیلتر)، `FinancialPosition` حالا `InlineNotice`+دکمه‌ی تلاش‌دوباره نشون می‌ده، و یک `InlineNotice` مشترک بالای «نیازمند توجه» برای سه ویجت که از یک fetch مشترک تغذیه می‌شن (`loadBranchCogsWaste` عمداً خارج از این ممیزی نگه داشته شد — تأثیرش محدودتره، فقط ستون COGS/waste SuperAdmin-only). بقیه‌ی رفع‌ها: تعویض toggle دستی داشبورد با `SegFilter` (کیبورد کامل)، focus-visible/`type="button"` روی ۶ آیتم منوی Popover در ۳ صفحه، حذف override بی‌اثر `h-9` روی ۴ تا `Select` تراکنش‌ها (چون `className` روی `<Select>` به `div` بیرونی می‌ره نه `<select>` واقعی — کشف معماری، نه رگرسیون)، یکسان‌سازی ارتفاع/focus-ring اینپوت بازه‌ی تاریخ، رنگ‌های hardcode مودال «دسته‌ی جدید» به توکن‌های theme، یکسان‌سازی فرمت‌کننده‌ی مبلغ پرشده از URL با `formatAmountInput`. **حذف کد مرده:** `components/dashboard/OperationsStrip.tsx` — کاملاً جایگزین‌شده با `OperationalQueues` از فاز قبل، دیگه از `index.ts` export نمی‌شد و هیچ‌جا import نمی‌شد (grep کل ریپو تأیید کرد، فقط در پوشه‌های release قدیمی مونده بود). **رگرسیون:** با `git diff cc87968 HEAD` روی فایل‌های حساس (`transactionsSlice.ts`، `balanceHelpers.ts`، `createExpenseTx.ts`، `lib/rbac.ts`، `lib/recruitment/notify.ts`) تأیید شد صفر تغییر در کل مسیر «Product UI V2» — فقط دو استثنای مستندشده از فازهای قبل (`trends/route.ts` رفع باگ RBAC واقعی، `branch-comparison/route.ts` endpoint جدید فقط‌خواندنی).
**فایل‌ها:** (ویرایش) `app/(app)/dashboard/page.tsx`، `components/dashboard/FinancialPosition.tsx`، `app/(app)/transactions/page.tsx`، `app/(app)/transactions/new/page.tsx`، `app/(app)/recruitment/page.tsx`، `components/recruitment/CandidateDetail.tsx` · (حذف) `components/dashboard/OperationsStrip.tsx` · (جدید) `project-docs/PRODUCT-UI-V2-AUDIT.md`
**Build:** tsc ✅ ۰ خطا · tests 420/420 ✅ · build ✅ · lint ✅ · playwright --list ✅ ۱۰۳ تست · git diff --check ✅ · اندازه‌ی route: `/dashboard` ۱۶.۷kB→۱۷kB، `/transactions` ۱۴.۹kB (بدون تغییر)، `/recruitment` ۱۴.۱kB (بدون تغییر)، `/transactions/new` ۶.۸۸kB (بدون تغییر) — رشد ناچیز، صفر dependency جدید (`git diff cc87968 HEAD -- package.json` خالی)
**ناتمام:** طبق دستور صریح تسک، merge به `main`/دیپلوی عمداً انجام نشد — فقط push روی `feat/product-ui-v2` (کامیت `3a86fbd`).
**برای جلسه‌ی بعد:** از کاربر تأیید بگیر برای merge/deploy این branch؛ گزارش کامل ممیزی در `project-docs/PRODUCT-UI-V2-AUDIT.md` هست. بعد از deploy، `loadBranchCogsWaste`ی بی‌صدا (یادداشت‌شده در بخش ۰) یک کاندید کوچک برای فاز بعدیه.

## 📓 2026-07-26 — ۳ اصلاح زنده روی «استخدام» بعد از بازبینی کاربر — merge/deploy شد
**چه شد:** کاربر بلافاصله بعد از دیدن نسخه‌ی زنده‌ی بازطراحی استخدام، با اسکرین‌شات واقعی ۳ درخواست داد: «باکس‌ها تراز نیست، مرتب کن»، «فیلتر اضافه کن که کامل‌تر تفکیک بشه، نه فقط زمان شروع»، «دکمه‌ی تماس بذار که مستقیم بره کال». **۱) تراز:** علت اصلی این بود که grid لیست داوطلبان `align-items` پیش‌فرض (`stretch`) داشت — کارت‌های کوتاه‌تر در یک ردیف به اندازه‌ی کارت بلندترین کشیده می‌شدند و فضای خالی نامتوازن ته‌شون می‌موند؛ با `items-start` روی خود grid رفع شد (هر کارت ارتفاع طبیعی خودش رو می‌گیره). علاوه بر این، نام داوطلب حالا `truncate` می‌شه (قبلاً wrap می‌کرد و کل ردیف‌های بعدی رو جابه‌جا می‌کرد)، ترتیب badgeها (وضعیت→بخش→سن) ثابت شد، و تگ‌های کلیدواژه به حداکثر ۳ تا محدود شدن. **۲) فیلتر جامع‌تر:** ماژول `filterCandidates.ts` با ۴ فیلتر جدید گسترش یافت — جنسیت، شیفت، کانال آشنایی، وجود/عدم‌وجود رزومه (هرکدوم با unit test جدید، جمعاً ۴ تست). به‌جای شلوغ‌کردن نوار فیلتر همیشه-دیده (که همین الان area/start/sort/جستجو داشت)، طبق همون الگوی «فیلتر پیشرفته» فاز تراکنش‌ها، این ۴ تا داخل یک `Popover` («فیلتر بیشتر») با badge تعداد قرار گرفتن — نه یک نوار شلوغ‌تر. همه در URL persist می‌شن. **۳) دکمه‌ی تماس:** قبلاً فقط داخل جزئیات باز‌شده‌ی کارت یک آیکون تماس کوچک بود؛ حالا مستقیم روی خود کارت جمع‌شده (کنار شماره) هم هست — یک `<a href="tel:...">` با `stopPropagation` که کارت رو باز نمی‌کنه. **باگ واقعی در همین حین پیدا و رفع شد:** الگوی قبلی (`<a href="tel:..."><IconButton/></a>`) یک `<button>` را داخل `<a>` می‌نشوند — HTML نامعتبر (دو المان تعاملی تودرتو، دو توقف Tab جدا برای یک اقدام). هم در کارت جدید و هم در جزئیات (که از فاز قبل همین الگو رو داشت) به یک `<a>` با استایل مستقیم (بدون nested button) تبدیل شد.
**فایل‌ها:** (ویرایش) `lib/recruitment/filterCandidates.ts` (۴ فیلتر جدید)، `tests/unit/filterCandidates.test.ts` (۴ تست جدید)، `app/(app)/recruitment/page.tsx` (Popover فیلتر پیشرفته، `items-start`)، `components/recruitment/CandidateCard.tsx` (بازچینی، دکمه‌ی تماس، truncate نام)، `components/recruitment/CandidateDetail.tsx` (رفع باگ nested button)، `tests/e2e/recruitment-redesign.spec.ts` (۳ تست جدید)
**Build:** tsc ✅ ۰ خطا · tests 420/420 ✅ (۴ جدید) · build ✅ · lint ✅ · playwright --list ✅ ۱۵ تست در این فایل (۳ جدید) · git diff --check ✅ · اندازه‌ی route: `/recruitment` ۱۳kB → ۱۴kB
**ناتمام:** با تأیید کاربر push شد → GH Actions run `30187626090`، attempt اول، موفق. **اصلاح دوم همون روز:** کاربر با اسکرین‌شات جدید گفت کارت‌ها هنوز «تراز و هم‌اندازه نیستن» — یعنی `items-start` قبلی مشکل رو حل نکرده بود، بلکه از یک مشکل (فضای خالی نامتوازن با stretch) به مشکل دیگه (کارت‌های واقعاً هم‌ارتفاع نبودن با start) سوییچ کرده بود. علت واقعی: ردیف اطلاعات عملیاتی (شماره/محله/شیفت/تاریخ) از `flex-wrap` استفاده می‌کرد — طول اسم محله («سپاه.انقلاب» در برابر «آجودانیه») باعث می‌شد هر کارت در نقطه‌ی متفاوتی بشکنه و تعداد خط متفاوتی داشته باشه، مستقل از رفتار grid بیرونی. رفع نهایی: آن ردیف به یک `grid grid-cols-2` ثابت (نه flex-wrap) تبدیل شد — ستون‌ها بر اساس طول متن reflow نمی‌کنن؛ و `items-start` از grid اصلی برداشته شد (برگشت به `stretch` پیش‌فرض) چون خواسته‌ی واقعی کاربر «هم‌اندازه» بود، نه فقط «بدون فضای خالی». با تأیید کاربر push شد → GH Actions run `30188651477`، attempt اول، موفق.
**برای جلسه‌ی بعد:** از کاربر بخواه تأیید کنه که کارت‌ها الان واقعاً هم‌اندازه‌ان، پنل «فیلتر بیشتر»، و دکمه‌ی تماس روی کارت جمع‌شده درست کار می‌کنن.

## 📓 2026-07-26 — merge + deploy بازطراحی «استخدام» روی main (v0.31.0)
**چه شد:** با تأیید صریح کاربر («دیپلوی کن»)، `feat/product-ui-v2` (آخرین commit: بازطراحی استخدام) روی `main` fast-forward شد و دیپلوی شد (GH Actions run `30175279169`، attempt اول، موفق). بلافاصله بعد از دیدن نسخه‌ی زنده، کاربر ۳ اصلاح خواست: تراز/چیدمان کارت‌های داوطلب، فیلترهای جامع‌تر (نه فقط زمان شروع)، و یک دکمه‌ی تماس مستقیم روی خود کارت جمع‌شده (نه فقط داخل جزئیات باز‌شده) — این‌ها در حال انجام‌ان (جزئیات در ورودی بعدی).
**فایل‌ها:** بدون تغییر کد — فقط عملیات git.
**Build:** CI کامل main: typecheck/lint/unit-test/deploy همه ✅.
**ناتمام:** ۳ اصلاح درخواستی بالا.
**برای جلسه‌ی بعد:** بعد از تکمیل ۳ اصلاح، دوباره با کاربر روی نسخه‌ی زنده مرور کن.

## 📓 2026-07-26 — بازطراحی کامل «استخدام» به یک workspace بررسی داوطلب — ادامه‌ی `feat/product-ui-v2`
**چه شد:** طبق یک اسپک ۸بخشی دقیق (حساس‌ترین بازطراحی این سری از نظر امنیتی — «شماره تماس/رزومه/پاسخ‌ها/یادداشت داخلی هرگز نباید به کاربر غیرمجاز، آنالیتیکس، لاگ یا payload اعلان درز کنه»)، صفحه‌ی `/recruitment` کامل بازطراحی شد. اول یک agent تحقیقی، صفحه‌ی ۸۹۲خطی فعلی، همه‌ی route های API، لایه‌ی RBAC/PII، و سیستم فرم‌ساز رو نقشه‌برداری کرد؛ یافته‌ی کلیدی امنیتی: کل صفحه از قبل با یک gate دوتایی (`user.role === 'SuperAdmin'`) محافظت می‌شه — نه لایه‌بندی نقشی میانی — و `lib/recruitment/notify.ts` از قبل (با تست regression موجود) فقط `firstName`/`lastName`/`area` رو به notification می‌فرسته، هرگز شماره/رزومه/پاسخ/یادداشت. **۱) اقدامات صفحه:** فرم عمومی (`/apply`) به اقدام اصلی (primary) تبدیل شد؛ «مقایسه» به‌عنوان اقدام contextual مستقیماً قابل‌مشاهده موند؛ اکسل/سوال‌ها/فرم‌ساز/دانلود رزومه به یک منوی `Popover` (role=menu) منتقل شدن — قبلاً ۶ دکمه هم‌وزن بودن. **۲) تب‌های خط‌لوله:** primitive کاملاً جدید `Tabs` (`components/ui/Tabs.tsx`) ساخته شد — قبلاً هیچ `role=tablist/tab/tabpanel` واقعی در کل پروژه نبود (فقط الگوی radiogroup در `SegFilter`/`Toggle`)؛ همون منطق ناوبری کیبورد تست‌شده (`nextRadioIndex`) دوباره‌استفاده شد. تعداد هر تب (همه/جدید/لیست‌کوتاه/قبول/رد) با اعمال بقیه‌ی فیلترها (نه فقط status) محاسبه می‌شه. فیلترهای بخش/زمان‌شروع/مرتب‌سازی/جستجو در یک `FilterToolbar` فشرده با چیپ‌های فعال + پاک‌سازی همه، و همه در URL persist می‌شن — ماژول خالص جدید `lib/recruitment/filterCandidates.ts` (۲۳ تست واحد) دقیقاً همون الگوی `filterTransactions.ts` فاز قبل رو برای این صفحه پیاده کرد. **۳) کارت داوطلب:** کامپوننت جدا `CandidateCard.tsx` — لیست از یک ستون تک‌عرض (که در صفحه‌ی عریض ۱۶۰۰px فضای خالی زیادی می‌ذاشت) به یک grid واقعی `auto-fit`/`minmax(360px,1fr)` تبدیل شد؛ کارت باز، تمام عرض grid رو می‌گیره (`col-span-full`) تا در ردیف چندستونه شکسته نشه. سه ناحیه‌ی هویت/عملیاتی/بررسی مجزا شدن؛ هدر کارت که قبلاً یک `<div onClick>` ساده بود حالا `role=button`+`tabIndex`+Enter/Space واقعی داره. **۴) حالت انتخاب یکپارچه:** قبلاً دو state جداگانه (`compareMode`+`compareIds` و `resumeSelectMode`+`selectedResumeIds`) با دو نوار تقریباً یکسان وجود داشت — یکی شدن به یک `selectedIds` + `SelectionToolbar.tsx` که هر دو اقدام (مقایسه، دانلود رزومه) رو هم‌زمان با غیرفعال‌سازی + توضیح («حداقل ۲ داوطلب انتخاب کنید»، «هیچ‌کدام رزومه ندارند») نشون می‌ده. **۵-۶) جزئیات داوطلب:** کامپوننت جدا `CandidateDetail.tsx` — هدر خلاصه (نام/بخش/وضعیت/امتیاز/رزومه + شماره تماس **فقط اگر** `canViewPhone(user)` — تابعی صریح و مرکزی در `filterCandidates.ts` که امروز معادل همون gate صفحه است ولی نقطه‌ی تغییر آینده رو مشخص می‌کنه)، سه تب خلاصه/سوال‌ها/رزومه (سوال‌وجواب حالا grid دوستونه در دسکتاپ با کنتراست بیشتر بین سوال و جواب، به‌جای بلوک‌های یک‌دست خاکستری)، و یک پنل بررسی همیشه-در‌دسترس (نه `position:sticky` واقعی — چون در یک لیست با چند کارت هم‌زمان باز، چند نوار «چسبیده به پایین صفحه» روی هم می‌افتادن؛ در عوض ناحیه‌ی محتوا خودش scroll محدود داره و پنل بیرون آن، همیشه دیده می‌شه). **باگ واقعی رفع شد:** `saveNote()` قبلی صرف‌نظر از موفقیت/شکست `reviewApplication()` همیشه toast موفقیت نشون می‌داد — یعنی اگه ذخیره‌ی یادداشت واقعاً fail می‌شد، کاربر هیچ‌وقت نمی‌فهمید. حالا `ReviewNoteField` وضعیت idle/saving/saved/error صریح داره. **حذف داوطلب** هم قبلاً بدون هیچ تأییدی فوری اجرا می‌شد — حالا داخل یک منوی خطر + `useConfirm()`. **۷) حالت‌ها:** فیلد `applicationsError` در `recruitmentSlice.ts` از قبل تعریف شده بود ولی هیچ‌جا ست نمی‌شد (کد مرده) — حالا واقعاً روی خطای بارگذاری ست می‌شه و با `InlineNotice` + دکمه‌ی تلاش دوباره نشون داده می‌شه؛ حالت «هنوز درخواستی نیست» از «نتیجه‌ای برای این فیلترها نیست» تفکیک شد؛ اسکلت loading اضافه شد (قبلاً در حین بارگذاری، حالت خالی غلط نشون داده می‌شد). یادداشت/کارت باز روی refresh پس‌زمینه یا خطای قابل‌بازیابی گم نمی‌شه (با همون الگوی `key={a.id}` بدون `reviewerNote` در key، از قبل درست بود، فقط حفظ شد). **۸) عملکرد:** بدون virtualization (مقیاس هنوز ده‌ها ردیف)؛ pagination/load-more موجود دست‌نخورده؛ هیچ fetch جدید per-candidate اضافه نشد (expand صرفاً toggle سمت کلاینت روی داده‌ی از‌قبل‌بارگذاری‌شده است) و فایل رزومه از قبل فقط با کلیک دانلود می‌شه (prefetch نمی‌شه) — تقسیم API لیست/جزئیات که بخشی از اسپک بهش اشاره داشت، عمداً انجام نشد چون با دستور صریح «APIها را حفظ کن» تناقض داشت.
**فایل‌ها:** (جدید) `components/ui/Tabs.tsx`، `components/recruitment/CandidateCard.tsx`، `CandidateDetail.tsx`، `SelectionToolbar.tsx`، `CompareModal.tsx`، `QuestionsModal.tsx`، `lib/recruitment/filterCandidates.ts`، `lib/recruitment/display.ts`، `tests/unit/filterCandidates.test.ts`، `tests/e2e/recruitment-redesign.spec.ts` · (ویرایش) `app/(app)/recruitment/page.tsx` (بازنویسی کامل)، `store/slices/recruitmentSlice.ts` (وصل‌کردن `applicationsError`)، `components/ui/index.ts` (export `Tabs`/`TabPanel`)
**Build:** tsc ✅ ۰ خطا · tests 416/416 ✅ (۲۳ جدید در `filterCandidates.test.ts`) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · playwright --list ✅ ۱۰۰ تست (۱۲ جدید در `recruitment-redesign.spec.ts`: تب‌ها، پایداری URL، مقایسه، PII، ذخیره‌ی یادداشت، ریسپانسیو، کیبورد، احراز هویت — اجرای واقعی مثل همیشه به‌خاطر نبود `.env.e2e` بلاک) · git diff --check ✅ · اندازه‌ی route: `/recruitment` قبل ۱۰.۱kB (First Load ۲۶۲kB) → بعد ۱۳kB (First Load ۲۶۵kB) — رشد ~۳kB برای مجموعه کامپوننت‌های جدید، منطقی
**ناتمام:** merge/deploy عمداً انجام نشد. بررسی بصری authenticated واقعی (باز/بستن کارت واقعی، تست تب‌ها با داده‌ی واقعی) این نشست هم دسترسی DB لوکال نداشت.
**برای جلسه‌ی بعد:** این branch رو با کاربر مرور کن؛ به‌خصوص روی داده‌ی واقعی تأیید کن که شماره تماس هنوز درست دیده می‌شه (فقط برای مدیر کل، که تنها نقش با دسترسی به این صفحه است) و حالت انتخاب یکپارچه (مقایسه+دانلود رزومه) گیج‌کننده نیست.

## 📓 2026-07-25 — هاتفیکس‌های بصری/اعتبارسنجی فرم «ثبت تراکنش» از بازبینی زنده‌ی کاربر (v0.31.0، مستقیم روی main)
**چه شد:** بعد از دیپلوی فرم جدید تراکنش، کاربر با اسکرین‌شات واقعی از production چند مشکل گزارش داد؛ دو دور اصلاح مستقیم روی `main` push شد (نه branch جدا — چون صفحه‌ی زنده بود و کاربر منتظر بود؛ هر دو با تأیید صریح کاربر قبل از push). **دور ۱ (بصری):** تاریخ روی آیکون تقویم می‌افتاد — باگ واقعی در `JalaliDatePicker.tsx`: آیکون با موقعیت‌دهی منطقی (`start-3`) نسبت به راست‌چین سند جای می‌گرفت، ولی خود input جهت `ltr` اجباری داشت، پس padding محافظ متن (`ps-10`) با جای واقعی آیکون هم‌خوانی نداشت؛ به physical `right-3`/`pr-10` تغییر کرد (مطابق الگوی `Input.tsx`) — این باگ روی همه‌ی فیلدهای تاریخ اپ اثر می‌ذاشت، نه فقط این فرم. باکس مبلغ از h-14/h-16 با فونت ۲۲-۲۶px به h-12/۱۷-۱۸px کاهش یافت. `StickyActionBar` (که برای چسبیدن به لبه‌ی کل `PageShell` طراحی شده، نه لبه‌ی یک `Card` مجزا) عریض‌تر از Card بالاش می‌زد بیرون — با `mx-0` هماهنگ شد. پیام ثابت تایید حذف شد. **دور ۲ (اعتبارسنجی — جدی‌تر):** کاربر گزارش داد اصلاً نمی‌تونه تراکنش ثبت کنه — خطای «Expected number, received string» زیر فیلد مبلغ. علت: برای فوکوس-روی-خطا فیلد مبلغ به react-hook-form با `register('amount')` وصل شده بود ولی فقط `onChange` override شده بود، نه `onBlur` — رفتار پیش‌فرض `onBlur` کتابخونه با خروج از فیلد، مقدار عددیِ درستی که با `setValue` دستی ست شده بود رو با رشته‌ی خام فرمت‌شده‌ی DOM بازنویسی می‌کرد. رفع شد: به‌جای spread کامل `register()`، فقط `ref`/`name` گرفته شد (برای `setFocus`)، بدون `onChange`/`onBlur` خودکار — چون مقدار این فیلد کاملاً دستی (`amountDisplay` + `setValue`) مدیریت می‌شه.
**فایل‌ها:** `app/(app)/transactions/new/page.tsx`، `components/ui/JalaliDatePicker.tsx`
**Build:** هر دو دور: tsc ✅ ۰ خطا · tests 393/393 ✅ · build ✅ · lint ✅ · git diff --check ✅ · دیپلوی هر دو دور موفق (GH Actions run `30169773351` و `30173005693`، هر دو attempt اول)
**ناتمام:** —
**برای جلسه‌ی بعد:** اگه کاربر دوباره «فرم کار نمی‌کنه» گزارش داد، اول کنسول مرورگر رو چک کن؛ این کلاس باگ (register()+custom onChange بدون override کردن onBlur) ممکنه جای دیگه‌ای هم توی فرم‌های آینده که از همین الگو استفاده می‌کنن تکرار بشه.

## 📓 2026-07-25 — merge + deploy فرم «ثبت تراکنش» — تکمیل کامل فاز «Product UI V2» روی main (v0.31.0)
**چه شد:** با تأیید صریح کاربر («merge/deploy کن»)، `feat/product-ui-v2` (آخرین commit: بازطراحی فرم ثبت تراکنش) روی `main` fast-forward شد — بدون conflict، چون `main` از قبل ancestor مستقیم branch بود (دو فاز قبلی همین branch — پایه‌ی UI و داشبورد و لیست تراکنش‌ها — قبلاً در نشست‌های جداگانه merge/deploy شده بودن). Push شد → GitHub Actions run `30168916747` اجرا شد: `typecheck`/`lint`/`unit-test` سبز، `deploy` (Liara) روی attempt اول موفق. با این merge، کل فاز چندروزه‌ی «Product UI V2» (پایه‌ی UI مشترک، بازطراحی IA داشبورد، بازطراحی لیست تراکنش‌ها، بازطراحی فرم ثبت تراکنش) به‌طور کامل روی production قرار گرفت؛ هیچ branch جداگانه‌ای دیگه در کار نیست.
**فایل‌ها:** بدون تغییر کد در این نشست — فقط عملیات git (`checkout main`، `merge --ff-only feat/product-ui-v2`، `push origin main`) + این فایل.
**Build:** CI کامل main (`run 30168916747`): typecheck ✅ · unit-test ✅ · lint ✅ · deploy به Liara ✅ (attempt ۱، بدون خطا).
**ناتمام:** بررسی بصری authenticated واقعی فرم جدید (ثبت یک هزینه/درآمد/انتقال واقعی روی خود production) هنوز با چشم کاربر تأیید نشده.
**برای جلسه‌ی بعد:** از کاربر بخواه یک تراکنش واقعی از `/transactions/new` روی سایت زنده امتحان کنه. اگه مشکلی دید سریع گزارش بده. بعدش: مهاجرت تدریجی بقیه صفحات (غیر از Dashboard/Transactions که الان تکمیل‌ان) به `PageShell`/`FilterToolbar`/`MetricGrid` — هنوز شروع نشده.

## 📓 2026-07-25 — بازطراحی فرم «ثبت تراکنش» — ادامه‌ی `feat/product-ui-v2`
**چه شد:** طبق یک اسپک ۷بخشی دقیق («وضوح، سرعت، پیشگیری از خطای مالی»)، صفحه‌ی `/transactions/new` بدون تغییر منطق حسابداری/تأیید/RBAC/API contract کاملاً بازطراحی شد. اول یک agent تحقیقی، صفحه‌ی فعلی، schema جدول `transactions`، منطق تأیید (نقش‌محور، نه آستانه‌ی مبلغ)، مکانیزم دوطرفه‌ی transfer (یک ردیف، دو account update)، و فرمول VAT رو کامل نقشه‌برداری کرد. **۱) اطلاعات اصلی:** `Toggle` (نوع تراکنش) از `role=group`+دکمه‌های مستقل (بدون کیبورد) به الگوی WAI-ARIA «Radio Group» کامل ارتقا یافت — دقیقاً همون منطق ناوبری (`nextRadioIndex`) که فاز قبل برای `SegFilter` ساخته و تست شده بود، این‌بار با ارتفاع لمسی ۴۴px (نه ۳۲px قبلی) چون این اولین و مهم‌ترین تصمیم فرمه. مبلغ به «قوی‌ترین فیلد» تبدیل شد — فونت ۲۲–۲۶px، ردیف hero مستقل، واحد «تومان» ثابت داخل فیلد؛ فرمت‌کردن ورودی همچنان از `formatAmountInput`/`parseAmount` موجود استفاده می‌کنه (بدون تغییر، از قبل مقدار عددی رو خراب نمی‌کرد). **۲) مسیر مالی:** روش پرداخت و طرف‌حساب (که قبلاً داخل accordion «جزئیات بیشتر» پنهان بودن با اینکه روش پرداخت اجباریه) به بخش همیشه-قابل‌مشاهده «مسیر مالی» منتقل شدن؛ برای انتقال وجه، صندوق مبدا/مقصد کنار هم نمایش داده می‌شن. **۳) تاریخ:** از داخل accordion به یک بخش مستقل و همیشه-دیده منتقل شد (چون فیلد اجباریه — نباید پشت یک کلیک اضافه پنهان باشه). **۴) مالیات/پیش‌فاکتور:** از پنل‌های بزرگ ستونی به ردیف‌های فشرده (چک‌باکس + پیش‌نمایش زنده‌ی مالیات در یک خط) تبدیل شدن؛ فرمول VAT دست‌نخورده موند (فقط UI). **۵) جزئیات بیشتر:** primitive جدید `Disclosure` (`components/ui/Disclosure.tsx`) ساخته شد — الگوی استاندارد WAI-ARIA Disclosure (`aria-expanded`+`aria-controls`+`hidden`، نه رندر شرطی) که تضمین می‌کنه مقدار فیلدهای داخلش (شماره رسید/کد فاکتور/توضیحات — تنها فیلدهای واقعاً اختیاری باقی‌مانده) هنگام جمع‌شدن هرگز گم نشه. **۶) ثبت:** primitive موجود ولی هرگز-استفاده‌نشده‌ی `StickyActionBar` بالاخره برای اولین بار در پروژه استفاده شد؛ خلاصه‌ی خطای قابل‌دسترس (`InlineNotice` role=alert) + فوکوس خودکار به اولین فیلد نامعتبر (`setFocus` از react-hook-form — نیازمند اضافه‌کردن `register('amount')` که قبلاً فقط state دستی داشت، بدون تغییر منطق)؛ محافظت double-submit با یک `ref` سنکرون (علاوه بر disabled شدن دکمه)؛ هشدار خروج بدون ذخیره با `useConfirm()` (نه `window.confirm`) + `beforeunload`؛ مقادیر فرم روی خطای سرور حفظ می‌شن (رفتار از قبل درست بود، حالا تضمین‌شده و تست‌شده). **۷) زمینه‌ی تأیید:** زیرنویس ضعیف «تراکنش شما فوری تایید می‌شود» با یک `InlineNotice` پویا جایگزین شد که بر اساس نقش کاربر و پرچم پیش‌فاکتور، پیامد واقعی (اثر روی موجودی صندوق) رو توضیح می‌ده — بدون تغییر منطق تأیید و بدون افشای جزئیات سیاست داخلی.
**فایل‌ها:** (جدید) `components/ui/Disclosure.tsx`، `tests/e2e/new-transaction-form.spec.ts` (۱۲ تست) · (ویرایش) `app/(app)/transactions/new/page.tsx` (بازنویسی کامل)، `components/ui/Toggle.tsx` (radiogroup+کیبورد)، `components/ui/index.ts` (export `Disclosure`)
**Build:** tsc ✅ ۰ خطا · tests 393/393 ✅ (بدون تغییر — منطق خالص جدیدی اضافه نشد، `Toggle` از تابع تست‌شده‌ی موجود دوباره‌استفاده کرد) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · playwright --list ✅ (۱۲ تست جدید در `new-transaction-form.spec.ts` قابل discover، اجرای واقعی به‌خاطر نبود `.env.e2e` مثل همیشه بلاک) · git diff --check ✅ · اندازه‌ی route: `/transactions/new` قبل ۵.۹۹kB (First Load ۱۸۷kB) → بعد ۶.۹۶kB (First Load ۱۸۹kB) — رشد ~۱kB برای `Disclosure`/`StickyActionBar`/`InlineNotice`/`PageHeader`/`PageShell`/`Checkbox`/`useConfirm`، منطقی
**ناتمام:** merge/deploy عمداً انجام نشد. بررسی بصری authenticated واقعی (پر کردن و ثبت یک تراکنش واقعی با چشم کاربر) این نشست هم دسترسی DB لوکال نداشت — dev server فقط تا ریدایرکت به `/login` بدون خطای کنسول تأیید شد.
**برای جلسه‌ی بعد:** این branch (`feat/product-ui-v2`) رو با کاربر مرور کن؛ ترجیحاً یک تراکنش واقعی (هزینه، درآمد، و انتقال وجه) از فرم جدید در محیط زنده ثبت کن تا مسیر مالی/VAT/پیش‌فاکتور با چشم تأیید بشه — قبل از merge/deploy.

<!-- archived to project-docs/handoff-archive.md: 2026-07-14 payroll approve + تأیید نهایی فاز ۱ v0.24.0 + 2026-07-14 فاز امنیتی ۱ + 2026-07-15 استقرار جریان تک‌نشست + 2026-07-15 تست رگرسیون P&L + 2026-07-15 جداسازی E2E + 2026-07-15 notification فاز ۱ + 2026-07-16 Notification Center V2 اولیه + 2026-07-16 اصلاح ۱۵‌گانه NC V2 + 2026-07-16 ممیزی مستقل ۹‌بخشی NC V2 + 2026-07-16 تست‌های رفتاری NC V2 + 2026-07-16 تکمیل کامل NC V2 (۱۱ بخش) + 2026-07-17 اصلاح release-blocker + 2026-07-18 NC V2 در production + scheduler داخل‌پروسه + 2026-07-18 پنل Super Admin موبایل‌فرندلی + 2026-07-18 کنترل گیرندگان اعلان + 2026-07-19 release کنترل گیرندگان اعلان + 2026-07-19 رفع باگ بحرانی crash کامل سایت + دانلود دسته‌ای رزومه + 2026-07-23 ESLint در CI + شروع UI/UX Refresh Phase 1 + 2026-07-23 اصلاح متمرکز باگ‌های موبایل و RTL + 2026-07-23 تکمیل UI/UX Refresh Phase 1 + 2026-07-24 Product UI V2 پایه + 2026-07-25 بازطراحی لیست تراکنش‌ها + 2026-07-24 بازطراحی داشبورد + 2026-07-25 merge/deploy کامل feat/product-ui-v2 روی main -->

