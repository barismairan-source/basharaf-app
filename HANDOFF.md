# HANDOFF.md — دفتر وضعیت زنده‌ی پروژه «با شرف»

> قانون طلایی: **هر جلسه اول بخش ۰ را بخوان؛ آخر جلسه بخش ۰ + ژورنال را به‌روز کن و commit/push کن.**
> جزئیات تاریخی قدیمی‌تر: `project-docs/handoff-archive.md`.

---

## ⚡ بخش ۰ — وضعیت لحظه‌ای (اول این را بخوان)

| | |
|---|---|
| **نسخه** | `main` روی `b597a77` — یکپارچه‌سازی «منابع انسانی» (۱۲ فاز) merge+deploy شد. کاربر خودش ۵ migration را قبلش در دیتابیس واقعی اجرا کرده بود، سپس صریحاً «push و merge و دیپلوی کن» گفت. |
| **آخرین به‌روزرسانی** | 2026-08-05 |
| **Build/tsc** | روی `main` (`b597a77`): tsc ✅ ۰ خطا · tests 548/548 ✅ (۲۹ تست جدید این نشست) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · playwright --list ✅ ۱۰۳ تست (بدون تغییر) · git diff --check ✅ |
| **دیپلوی** | ✅ موفق — GH Actions run `31043464598` («Deploy to Liara»)، conclusion=success. با curl مستقیم روی basharaf.me تأیید شد: صفحه‌ی اصلی ۲۰۰، `/hr` بدون session ۳۰۷ (نه کرش)، `/employees` واقعاً به `/login?redirect=%2Fhr%2Fpeople` ریدایرکت می‌شود (یعنی مسیر قدیمی زنده است)، `/api/hr/overview` و `/api/hr/payroll/readiness` بدون session ۴۰۱ می‌دهند (نه ۵۰۰/کرش). |
| **کار نیمه‌تمام (in-progress)** | یکپارچه‌سازی HR روی production است ولی یک چرخه‌ی کامل end-to-end (استخدام→تبدیل به پرسنل، ثبت شیفت/حضور در `/hr/time`، محاسبه‌ی حقوق در `/hr/payroll`) هنوز با کاربر واقعی روی production تست نشده. سیستم حقوق ساعتی (نشست قبل‌تر) هم هنوز منتظر است: نرخ ساعتی/قالب شیفت واقعی برای هیچ کارمندی تعریف نشده. |
| **کار بعدی پیشنهادی** | Smoke test دستی طبق چک‌لیست `project-docs/HR-UNIFICATION-ROLLOUT.md` بخش ۶ (ورود SuperAdmin، هرکدام از مسیرهای قدیمی، `/hr/people`، `/hr/time` هر ۴ تب، یک استخدام تستی «تبدیل به پرسنل»). بعد از آن: همان چرخه‌ی حقوق ساعتی (نرخ→شیفت→حضور→محاسبه). |
| **یافته‌ی مهم این نشست** | حین بررسی کد قبل از پیاده‌سازی، مشخص شد `PATCH /api/recruitment/[id]` از قبل رفتار مستندی داشت که با تغییر وضعیت به `accepted` یک کارمند می‌ساخت (idempotent فقط با شماره‌تلفن، بدون قفل، بدون ارتباط ذخیره‌شده به متقاضی). این رفتار عمداً حذف و با یک عملیات اتمیک جدید (`POST /api/hr/recruitment/[id]/hire`) جایگزین شد — جزئیات کامل در `project-docs/HR-UNIFICATION-IMPLEMENTATION.md` فاز ۷. برای رکوردهای قبل از این فاز، کوئری تشخیص کارمند تکراری در `HR-UNIFICATION-ROLLOUT.md` بخش ۸ موجود است — هنوز اجرا/بررسی نشده. |
| **بلاک‌شده/منتظر کاربر** | ۱) Playwright e2e ⛔ — `.env.e2e` غایب (بدون تغییر، چند ماهه)؛ ۴ فایل e2e موجود (`payroll.spec.ts`, `recruitment-redesign.spec.ts`, `product-ui-v2.spec.ts`, `mobile-ux-audit-fixes.spec.ts`) به مسیرهای قدیمی (`/payroll`, `/recruitment`) اشاره می‌کنند — چون assertionهاشون regex/substring است (نه exact path match)، احتمالاً بعد از redirect هم پاس می‌شوند ولی این با اجرای واقعی تأیید نشده. ۲) منشأ `below_approval_limit.email_enabled=true` هنوز چک نشده. ۳) Web Push هنوز روی هیچ مرورگر واقعی تست نشده. ۴) ۵ تصمیم مالک در `HR-UNIFICATION-ROLLOUT.md` بخش ۱۰ (مثلاً آیا `job_titles` جایگزین تنظیمات JSON فعلی شود). ۵) smoke test دستی end-to-end (بالا). |

> ⚠️ **نکته مهم برای جلسات بعدی:** فرم `/apply` حالا کاملاً داینامیک و دیتابیس‌محور است. **دیگر فیلد hard-code به `app/apply/page.tsx` یا `lib/recruitment/` اضافه نکنید.** همه فیلدهای جدید باید از طریق `/recruitment/form-builder` ایجاد شوند.

> ⚠️ **مستندات investigation قدیمی را کورکورانه باور نکنید:** `project-docs/financial-integrity-spec.md`، `INVESTIGATION-full-project-review.md`، `domain-audit.md`، `inventory-audit.md` گزارش‌های تاریخی‌اند — بخش زیادی از گپ‌هایی که توصیف می‌کنن از قبل رفع شده (تأییدشده در نشست ۲۰۲۶-۰۷-۲۷). قبل از اقدام روی هر ادعای این اسناد، حتماً با `grep`/خواندن مستقیم کد فعلی تأیید کنید — یک هشدار مشابه بالای خودِ این فایل‌ها هم اضافه شده.

---

## 📋 پروتکل جلسه

### شروع هر جلسه (الزامی — به ترتیب)
⛔ **تنها یک نشست فعال Claude Code مجاز است روی این پوشه ویرایش کند — دو نشست همزمان باز نکن.**
1. این فایل، بخش ۰ + جدیدترین ورودی ژورنال را بخوان.
2. `git status` بزن — اگر تغییرات commit‌نشده هست، یعنی جلسه‌ی قبل ناقص بسته شده: اول وضعیت را با کاربر روشن کن، چیزی را کورکورانه commit یا revert نکن.
3. `git log -5 --oneline` — تطبیق بده با ژورنال.
4. به کاربر خلاصه بگو: «وضعیت X است، کار نیمه‌تمام Y، پیشنهاد بعدی Z» و منتظر تأیید بمان.

### پایان هر جلسه / بعد از هر تغییر معنادار (الزامی)
1. یک ورودی ژورنال با **قالب زیر** به بالای بخش ژورنال اضافه کن.
2. بخش ۰ را به‌روز کن (نسخه، تاریخ، کار نیمه‌تمام، کار بعدی).
3. اگر ژورنال بیش از **۷ ورودی** شد، قدیمی‌ها را به `project-docs/handoff-archive.md` منتقل کن.
4. `git add -A && git commit -m "..." && git push` — **بدون push جلسه را نبند.**
5. ⛔ **دیگر ZIP نساز** — GitHub Actions خودکار deploy می‌کند (از 2026-06-24).

### قالب اجباری ورودی ژورنال
```markdown
## 📓 [تاریخ] — [عنوان کوتاه]
**چه شد:** (۲–۵ خط، تصمیم‌های مهم + چرایی)
**فایل‌ها:** (مسیر کامل ایجاد/ویرایش‌شده‌ها)
**Build:** tsc/build سبز یا خطا + متن خطا
**ناتمام:** دقیقاً کجا متوقف شد، چه چیزی نیمه‌کاره است (اگر هیچ: «—»)
**برای جلسه‌ی بعد:** کار بعدی مشخص + هر هشداری که جلسه‌ی بعد باید بداند
```

---

## 🏗 بخش ۴ — مرجع پایدار (قبل از دست‌زدن به مالی بخوان)

- `accounts.balance` فیلد **cache** است: همیشه از reverse transaction‌ها محاسبه شده. هر تراکنش معکوس جا مانده = drift دائمی.
- ⚠ **INVARIANT فاز ۵**: `accounts.balance` هرگز توسط فیلتر `is_setup` (لنز عملیاتی) تغییر نمی‌کند. این فیلتر فقط روی KPI‌های جریان مالی (income/expense/profit) در داشبورد و گزارش‌ها اثر دارد — ترازنامه مستقل است.
- پول **bigint تومان** در DB؛ در TypeScript `number` (JS safe integer تا ۹۰۰۰ تریلیون کافی است).
- تاریخ کاربری **جلالی text** (مثل `۱۴۰۳-۰۴-۱۵`)؛ تاریخ داخلی `timestamp` میلادی.
- `JWT_SECRET` حتماً ≥۳۲ کاراکتر.
- فرم `/apply` کاملاً داینامیک — فیلد hard-code اضافه نکن.

---

## 📓 ژورنال نشست‌ها (جدیدترین بالا — حداکثر ۷ ورودی)

## 📓 2026-08-05 — merge + deploy یکپارچه‌سازی «منابع انسانی» روی main
**چه شد:** کاربر تأیید کرد که ۵ migration فاز HR را خودش قبلاً روی دیتابیس واقعی اجرا کرده، و صریحاً گفت «push و merge و دیپلوی کن». چون `main` محلی دقیقاً با `origin/main` یکی بود، branch به‌صورت fast-forward (بدون merge commit) push و merge شد؛ push روی `main` GH Actions run `31043464598` را trigger کرد که با موفقیت تمام شد. بعد از deploy با curl مستقیم روی `basharaf.me` تأیید شد: صفحه‌ی اصلی ۲۰۰، `/hr` بدون session ۳۰۷ (نه کرش)، `/employees` واقعاً به `/login?redirect=%2Fhr%2Fpeople` ریدایرکت می‌شود، و ۲ API جدید (`/api/hr/overview`, `/api/hr/payroll/readiness`) بدون session درست ۴۰۱ می‌دهند.
**فایل‌ها:** بدون تغییر کد — فقط عملیات git (push branch + fast-forward merge + push main) + یک commit مستندسازی HANDOFF.
**Build:** CI کامل main (deploy workflow): موفق. تست/build محلی از قبل روی همین commit سبز بود (548/548).
**ناتمام:** smoke test دستی end-to-end هنوز با کاربر واقعی انجام نشده (چک‌لیست در `HR-UNIFICATION-ROLLOUT.md` بخش ۶). ۵ تصمیم مالک در همان سند بخش ۱۰ باز مانده.
**برای جلسه‌ی بعد:** از کاربر بخواه یک استخدام تستی را «تبدیل به پرسنل» کند و یک چرخه‌ی شیفت→حضور→تأیید→محاسبه را در `/hr/time` و `/hr/payroll` روی داده‌ی واقعی امتحان کند.

## 📓 2026-08-05 — یکپارچه‌سازی کامل «منابع انسانی» در ۱۲ فاز — branch جدا `feat/hr-workspace-unification`
**چه شد:** طبق یک دستور بسیار مفصل («استخدام، پرسنل، شیفت، حضور و حقوق را به یک ماژول «منابع انسانی» یکپارچه تبدیل کن»)، در ۱۲ فاز متوالی (هرکدام commit محلی مستقل) کل این ۵ حوزه زیر `/hr/*` یکپارچه شد. **فاز ۰:** ممیزی کامل — نقشه‌ی مسیرهای فعلی، کشف این‌که `/shift-schedule`/`/attendance` اصلاً در `PROTECTED_PREFIXES` نبودند، کشف وابستگی گزارش‌های مالی به رشته‌ی لفظی «حقوق پرسنل». **فاز ۱ (قبل از هر بازطراحی ظاهری، طبق دستور):** جلوگیری واقعی سمت‌سرور (درون transaction) از هم‌پوشانی حضور — قبلاً فقط UI جلو می‌گرفت؛ `employees.compensationType` صریح جایگزین حدس‌زدن از روی وجود نرخ ساعتی؛ موتور مشترک آمادگی حقوق (`computeReadinessForEmployees`) که هم API گزارش و هم گیت‌های calculate/approve از آن استفاده می‌کنند. **فاز ۲:** section جدید `hr` + ۱۹ کلید `hr.*` در سیستم دسترسی granular موجود (نه یک مدل موازی)، با پل سازگاری برای permissionهای قدیمی. **فاز ۳:** جابه‌جایی فیزیکی صفحات (`git mv`، صفر تغییر منطق) به `/hr/people`, `/hr/payroll`, `/hr/recruitment`؛ ادغام UI شیفت+حضور در `/hr/time` (Tabs، داده‌ها جدا مانده)؛ redirect مسیرهای قدیمی در middleware (query string حفظ می‌شود) — با dev server واقعی تأیید شد. **فاز ۴:** نمای کلی `/hr` با یک API خلاصه‌ی واحد (نه فچ جدا برای هر کارت). **فاز ۵:** فیلتر/جست‌وجو در فهرست افراد + پرونده‌ی ۳۶۰ درجه (۹ تب — ۴ تای واقعی، ۵ تای صادقانه placeholder چون یا زیرساخت‌شان نبود مثل مدارک، یا به فازهای بعدی وابسته بودند)؛ جدول `job_titles` (فقط زیرساخت، هنوز جایگزین تنظیمات JSON فعلی نشده). **فاز ۶:** دو تب جدید واقعی («تأییدها» صف واحد بررسی، «گزارش کارکرد» جدول per-employee). **فاز ۷ (یافته‌ی مهم):** کشف یک رفتار مستند قدیمی که با تغییر وضعیت متقاضی به «قبول» به‌طور ضمنی (بدون قفل، بدون ردیابی) یک کارمند می‌ساخت — چون همزمانی‌اش با دکمه‌ی جدید «تبدیل به پرسنل» می‌توانست یک متقاضی را دوبار به کارمند تبدیل کند، این رفتار قدیمی حذف و با یک عملیات اتمیک واقعی جایگزین شد؛ enum جدید `hired` هم عمداً اضافه نشد (ریسک/گستره‌ی غیرضروری) — به‌جایش `hiredAt` تمایز accepted/hired را می‌دهد. **فاز ۸:** اتصال اختیاری `employees.userId`، با استفاده مجدد از endpoint موجود ساخت کاربر (بدون تکرار منطق رمز). **فاز ۹:** پنل آمادگی + نوع محاسبه‌ی صریح در فهرست فیش‌ها. **فاز ۱۰:** حذف کامل `window.confirm` از کل ماژول HR (۹ فراخوانی، `useConfirm` موجود پروژه). **فاز ۱۱:** مستند rollout کامل (ترتیب migration، smoke test، تصمیم‌های باقی‌مانده‌ی مالک).
**فایل‌ها:** ~۶۰ فایل (جدید/جابه‌جا/ویرایش) — فهرست کامل در `project-docs/HR-UNIFICATION-IMPLEMENTATION.md` (هر فاز بخش جدا دارد). مهم‌ترین‌ها: `lib/auth/permissions.ts`، `middleware.ts`، `app/(app)/hr/**`، `components/hr/**`، `lib/payroll/payrollReadiness.ts`، ۵ migration جدید (`db-hr-*.sql`)، `project-docs/HR-UNIFICATION-ROLLOUT.md` (جدید).
**Build:** tsc ✅ ۰ خطا · tests 548/548 ✅ (۲۹ جدید: hr-permissions ۱۵ + hr-legacy-redirects ۷ + findAttendanceOverlap ۷) · lint ✅ (فقط ۲ warning preexisting بی‌ربط) · build ✅ · playwright --list ✅ ۱۰۳ تست (بدون تغییر) · git diff --check ✅ · صفر dependency جدید.
**ناتمام:** merge/push/deploy/migration عمداً انجام نشد (محدودیت صریح تسک). ۳ محدودیت آگاهانه مستند شده‌اند: نمای هفتگی شبکه‌ای واقعی برای برنامه شیفت ساخته نشد، یکسان‌سازی کامل سه الگوی container صفحات انجام نشد، مدیریت مدارک پرسنل (کل قابلیت، نه فقط این فاز) هنوز وجود ندارد. ۴ فایل e2e موجود به مسیرهای قدیمی اشاره می‌کنند (جزئیات در بخش ۰).
**برای جلسه‌ی بعد:** چک‌لیست کامل در `project-docs/HR-UNIFICATION-ROLLOUT.md`. خلاصه: migration دستی (۵ فایل، ترتیب مستند شده) → تست دستی طبق smoke test → تصمیم merge.

## 📓 2026-08-05 — تبدیل کامل سیستم حقوق ماهانه به ساعتی+شیفت+حضور — branch جدا `feat/hourly-attendance-payroll`
**چه شد:** طبق یک اسپک بسیار مفصل (سه مفهوم جدا: شیفت برنامه‌ریزی‌شده/حضور واقعی/حقوق قابل‌پرداخت)، سیستم حقوق ساعتی به‌طور کامل و **افزودنی** (بدون حذف سیستم ماهانه‌ی موجود) ساخته شد. قبل از هر تغییری، `payrollEngine.ts`، `postToBasharaf.ts`، صفحات پرسنل/حقوق، calculate route، و schema فعلی کامل خوانده شدند. **هسته‌ی محاسبه:** `lib/payroll/attendanceEngine.ts` (پیوره، بدون DB) — تبدیل ساعت↔دقیقه، سیاست استراحت (paid/unpaid/none)، تفکیک عادی/اضافه‌کاری (مبنا=`plannedMinutes` روز، حضور بیشتر هرگز حذف نمی‌شود)، تشخیص شب‌کاری با هم‌پوشانی بازه (پشتیبانی کامل از عبور نیمه‌شب)، resolve نرخ ساعتی بر اساس تاریخ (بدون اثر روی گذشته)، تشخیص هم‌پوشانی شیفت، ماشین‌حالت `draft→confirmed→locked`، و `calculateHourlyPayslip` که **هر روز را با نرخ همان روز جداگانه محاسبه و بعد جمع می‌زند** (نه جمع دقایق با یک نرخ). ۴۴ تست واحد نوشته شد که ۲۵ سناریوی اصلی خواسته‌شده را پوشش می‌دهد (شیفت ۶/۸/سفارشی، snapshot تخصیص، اضافه‌کاری تأییدشده/نشده، عبور نیمه‌شب، دو روش ثبت، نرخ چندگانه/تغییر میان‌ماه، انواع مرخصی/غیبت، حضور بدون شیفت، هم‌پوشانی، جلوگیری از کسر دوگانه). دو باگ واقعی در همین فاز پیدا و رفع شد: `.sort()` پیش‌فرض روی اعداد (لغزش تست، نه موتور) و `shiftRangesOverlap` که هم‌پوشانی شیفت‌های عبورکننده از نیمه‌شب با شیفت روز بعد را تشخیص نمی‌داد (با مقایسه‌ی offset ±۱۴۴۰ رفع شد). **schema:** ۴ جدول جدید (`shift_templates`، `employee_hourly_rates`، `employee_shift_assignments` با snapshot کامل از قالب، `attendance_entries`) + ۵ enum، دقیقاً طبق فیلدهای خواسته‌شده در `lib/db/schema.ts` + migration idempotent جدا (`db-hourly-attendance-payroll.sql`، اجرا نشده). **API:** ۹ مسیر جدید (shift-templates CRUD، hourly-rates با overlap-check درون‌تراکنشی، shift-assignments با پشتیبانی bulk/copy چندکارمندی×چندروزه و گزارش conflicts، attendance با محاسبه‌ی سروری همیشگی مشتقات (هرگز به مقادیر مالی کلاینت اعتماد نمی‌شود) + confirm/confirm-bulk). **یکپارچه‌سازی با سیستم موجود (بدون شکستن آن):** `calculate/route.ts` حالا per-employee تشخیص می‌دهد کارمند نرخ ساعتی دارد یا نه — اگر دارد از `attendance_entries` تأییدشده‌ی DB (نه body) می‌خواند، اگر ندارد دقیقاً مسیر ماهانه‌ی قبلی؛ یک اجرا می‌تواند مخلوط هر دو باشد. `approve/route.ts` اگر حضور draft باقی‌مانده پیدا کند رد می‌کند. `postToBasharaf.ts` بعد از post، حضورهای همان دوره را `locked` می‌کند و در reverse برمی‌گرداند — منطق حسابداری/idempotency/دسته‌ی «حقوق پرسنل» دست‌نخورده ماند. **UI:** دو صفحه‌ی جدید (`/shift-schedule` نمای روزانه/هفتگی+کپی+قالب سریع ۶/۸ساعت+هشدار هم‌پوشانی، `/attendance` با پیش‌نمایش لحظه‌ای محاسبه با import مستقیم توابع خالص در کلاینت — بدون round-trip) + گسترش `/employees` (مدیریت نرخ ساعتی: فعال/سابقه/ثبت جدید). **اسکریپت پاک‌سازی:** `scripts/reset-payroll-to-hourly.ts` — dry-run پیش‌فرض، فقط `payroll_runs` غیر-posted+`payslips` مرتبط، هرگز به employees/مدارک/بانکی/حسابداری دست نمی‌زند.
**فایل‌ها:** (جدید) `lib/payroll/attendanceEngine.ts`، `lib/payroll/attendanceEntryHelpers.ts`، `tests/unit/attendanceEngine.test.ts`، `project-docs/migrations/db-hourly-attendance-payroll.sql`، `project-docs/HOURLY-ATTENDANCE-PAYROLL.md`، `scripts/reset-payroll-to-hourly.ts`، `app/api/shift-templates/route.ts`+`[id]/route.ts`، `app/api/employees/[id]/hourly-rates/route.ts`، `app/api/shift-assignments/route.ts`+`[id]/route.ts`، `app/api/attendance/route.ts`+`[id]/route.ts`+`[id]/confirm/route.ts`+`confirm-bulk/route.ts`، `app/(app)/shift-schedule/page.tsx`، `app/(app)/attendance/page.tsx`، `store/slices/hourlyPayrollSlice.ts` · (ویرایش) `lib/db/schema.ts` (۴ جدول+۵ enum)، `lib/jalali.ts` (`jalaliMonthRange`)، `lib/payroll/postToBasharaf.ts` (قفل/بازکردن حضور)، `app/api/payroll/runs/[id]/calculate/route.ts`، `app/api/payroll/runs/[id]/approve/route.ts`، `app/(app)/employees/page.tsx`، `components/layout/nav-config.ts`، `store/index.ts`، `package.json` (اسکریپت npm جدید)
**Build:** tsc ✅ ۰ خطا · tests 519/519 ✅ (۴۴ جدید) · lint ✅ (فقط warningهای preexisting بی‌ربط) · build ✅ · playwright --list ✅ ۱۰۳ تست (بدون تغییر — طبق دستور صریح تسک، بدون دیتابیس production تست مرورگر جدید نوشته نشد، فقط مستند شد) · git diff --check ✅
**ناتمام:** روند تأیید سه‌مرحله‌ای کامل شد: ۱) گزارش اولیه بدون push. ۲) کاربر گفت «همه آپدیت‌ها انجام شد پوش کن» → commit `0361453` روی branch جدا push شد. ۳) کاربر گفت «دیتابیس‌ها و میگریشن‌ها انجام شد دیپلوی کن» (یعنی خودش migration را روی دیتابیس واقعی اجرا کرده بود) → fast-forward merge به `main` (`61e3d04`→`832cae5`) + push به `origin/main` انجام شد که GH Actions را تریگر کرد. **دیپلوی تأیید شد:** run `30950056551` conclusion=success؛ با curl مستقیم روی basharaf.me هم صفحه‌ی اصلی (۲۰۰) و هم دو مسیر API جدید بدون session (۴۰۱ درست، نه کرش) چک شد. هیچ نرخ ساعتی/قالب شیفت واقعی هنوز برای هیچ کارمندی تعریف نشده (همه‌ی کارمندان فعلاً خودکار در مسیر ماهانه‌ی قبلی می‌مانند — بی‌خطر). پیش‌نمایش کلاینت در `/attendance` فقط دقیقه نشان می‌دهد نه مبلغ تومانی (تصمیم آگاهانه — جزئیات در سند معماری بخش ۱۱).
**برای جلسه‌ی بعد:** جزئیات کامل + چک‌لیست در `project-docs/HOURLY-ATTENDANCE-PAYROLL.md` بخش ۱۶. خلاصه‌ی باقیمانده: از صفحه‌ی «پرسنل» برای حداقل یک کارمند نرخ ساعتی واقعی تعریف شود، از «برنامه شیفت» یک شیفت واقعی ثبت شود، از «حضور و غیاب» ثبت/تأیید شود، بعد یک اجرای حقوق واقعی محاسبه/تأیید/ثبت شود — تا کل مسیر روی داده‌ی واقعی production تست شود.

## 📓 2026-07-27 — merge/deploy کانال Push روی main + بررسی «چه چیزی مونده» و کشف stale بودن ۴ سند investigation قدیمی
**چه شد:** با تأیید کاربر (پیام تکراری «این دیپلوی و فیکس شد و اضافه شد» — مبهم بود، بعد از بلاک‌شدن توسط هارنس با `AskUserQuestion` تفسیرم تأیید شد)، `feat/web-push-notifications` روی `main` merge/fast-forward و دیپلوی شد (GH Actions run `30447781769`، attempt اول، موفق؛ کاربر خودش قبلش migration + ۴ متغیر VAPID رو در Liara تنظیم کرده بود). **تست بدون لاگین:** چون دسترسی production ندارم، فقط با curl/Browser pane چک شد — `sw-push.js` سرو می‌شه (۲۰۰)، `/api/push/subscribe` روی GET درست ۴۰۵ می‌ده (نه کرش، چون فقط POST داره)، `/api/admin/notifications/provider-status` بدون لاگین درست ۴۰۱ می‌ده — یعنی دیپلوی سالمه؛ تست واقعی توگل مرورگر فقط از دست کاربره. **بعدش کاربر پرسید «چه چیزی کلاً از پروژه مونده».** یک agent تحقیقی روی اسناد قدیمی (`financial-integrity-spec.md`/`INVESTIGATION-full-project-review.md`/`domain-audit.md`/`inventory-audit.md`) گزارش داد ۷ مشکل «بحرانی/بالا» (برگشت‌ناپذیری COGS هنگام حذف تراکنش، نبود audit log روی ۶ عملیات مالی، نبود rate limit OTP، چاپ کد OTP در production، نبود دکمه‌ی بازنشانی اجباری حقوق، نبود food cost %، نبود انتخاب صندوق در تأیید خرید). **به‌جای اعتماد به گزارش، هر مورد رو شخصاً با کد فعلی چک کردم** (طبق همون درسی که این نشست چندبار دیگه هم گرفته بودم — هیچ‌وقت خلاصه‌ی agent رو بدون چک منبع باور نکن): **هر ۷ مورد از قبل حل شده بودن** — `reverseSaleDeduction`+`voidCogsTransaction` کامل (`lib/db/balanceHelpers.ts`)، `audit()` روی هر ۶ عملیات هست، `checkOtpRateLimit`+`OTP_MAX_ATTEMPTS` پیاده‌سازی شده، لاگ OTP پشت `NODE_ENV!=='production'` گارد شده، دکمه‌ی «بازنشانی اجباری» توی UI حقوق هست، `foodCostPct` کامل با رنگ‌بندی ۳۰٪/۴۰٪ پیاده‌سازی شده، و مودال انتخاب صندوق تأیید خرید (`app/(app)/inventory/cartable/page.tsx:396-439`) هم از قبل کامل بود — یک grep اشتباه خودم اول این آخری رو جا انداخته بود، با خوندن مستقیم فایل تصحیح شد. تنها چیز باقی‌مانده: گروه سایدبار «روابط و منابع» (مشتری+پرسنل+حقوق+استخدام قاطی) — این فقط نظر دسته‌بندیه، نه باگ، اقدامی روش انجام نشد. **نتیجه‌گیری مهم:** ۴ سند investigation یادشده dokumentation قدیمی‌ان که گزارش‌شون دیگه معتبر نیست — یک هشدار دائمی در بخش ۰ همین فایل اضافه شد تا جلسات بعدی کورکورانه بهشون اعتماد نکنن.
**فایل‌ها:** فقط `HANDOFF.md` (بخش ۰ + این ورودی) و `project-docs/handoff-archive.md` (آرشیو ورودی قدیمی) — هیچ کد پروژه تغییر نکرد، چون هر ۷ مورد گزارش‌شده از قبل درست بود.
**Build:** بدون تغییر کد — گیت‌ها اجرا نشدن (نیازی نبود).
**ناتمام:** —
**برای جلسه‌ی بعد:** منتظر تست واقعی کاربر روی توگل «نوتیفیکیشن مرورگر». اگه خواست فاز بعدی رو مشخص کنه، بهترین کاندید واقعی همون گروه‌بندی سایدبار «روابط و منابع» است (نظر UX، نه باگ) — یا هر چیز دیگه‌ای که خودش بخواد، چون هیچ گپ مالی/امنیتی واقعی روی میز نیست.

## 📓 2026-07-27 — ۵ فیکس زنده‌ی ملی‌پیامک روی main + کانال Push کامل روی branch جدا `feat/web-push-notifications`
**چه شد:** بعد از دیپلوی provider ملی‌پیامک، کاربر مستقیم از پنل زنده تست کرد و ۳ خطای واقعی پشت‌سرهم پیدا شد — هر کدوم با تأیید کاربر مستقیم روی `main` push شد (چون تست زنده در حال انجام بود): **۱) قانون sms.test_notify هیچ راه UI برای روشن‌شدن نداشت** (عمداً از صفحه‌ی مرکزی اعلان‌ها مخفی بود) — یک Switch مستقل در SmsPane اضافه شد. **۲) دکمه‌ی تست نتیجه‌ی نادرست می‌داد** چون فقط outbox رو enqueue می‌کرد و ۶۰ ثانیه صبر لازم داشت (scheduler داخل‌پروسه) — الان همون‌جا `processOutboxBatch()` رو هم صدا می‌زنه تا نتیجه فوری باشه. **۳) پیامک واقعی رد می‌شد** — اول با خطای «متن حاوی لینک» (چون `buildSmsMessage` همیشه لینک actionUrl رو اضافه می‌کنه؛ برای ملی‌پیامک این لینک حذف شد، کاوه‌نگار دست‌نخورده موند)، بعد با «متن حاوی کلمه فیلتر شده» (چون متن تست شامل UUID خام + کلمات انگلیسی بود؛ به یک جمله‌ی ساده‌ی فارسی تغییر کرد). کاربر همچنان درخواست پیامک متنی کرد؛ تحقیق در مستندات رسمی ملی‌پیامک (`WebSearch`+`WebFetch`) نشون داد **خط خدماتی اشتراکی اصلاً از متن آزاد پشتیبانی نمی‌کنه** — پترن/قالب از پیش تأییدشده لازمه؛ چون این نیازمند ثبت پترن در پنل توسط خود کاربره (اقدام دستی، خارج از کنترل کد)، به‌جاش یک بخش «تست متن دلخواه» به SmsPane اضافه شد (endpoint `POST /api/sms/test` از قبل بدون UI بود، الان وصل شد) تا کاربر خودش بتونه بدون deploy مجدد متن‌های مختلف رو امتحان کنه. **بعدش:** کاربر خواست «نوتیفیکیشن» (Push واقعی مرورگر، نه فقط زنگوله‌ی داخل‌اپ) اضافه بشه — روی branch جدا `feat/web-push-notifications` کامل ساخته شد: جدول `push_subscriptions` + ستون `notification_rules.push_enabled`، آداپتور `lib/notifications/channels/push.ts` (چندین دستگاه هر کاربر، پاک‌سازی خودکار subscriptionهای منقضی ۴۰۴/۴۱۰)، وصل‌شدن کامل به همون pipeline موجود outbox/audience/rules که SMS/ایمیل ازش استفاده می‌کنن (نه سیستم موازی جدا)، service worker (`public/sw-push.js`)، توگل self-serve در تنظیمات → تنظیمات سامانه → اعلان‌ها (برای همه‌ی نقش‌ها، نه فقط مدیر کل)، و چک‌باکس «نوتیفیکیشن» در پنل مدیریت قوانین/drawer گیرندگان (دقیقاً مطابق الگوی پیامک/ایمیل موجود). کلیدهای VAPID یک‌بار با `web-push generate-vapid-keys` تولید شد (رایگان، بدون نیاز به اکانت شخص ثالث برخلاف SMS).
**فایل‌ها:** (فیکس‌های ملی‌پیامک، روی main) `components/settings/SmsPane.tsx`، `app/api/sms/test-notify/route.ts`، `lib/notifications/processor.ts` (پارامتر `includeLink`) · (Web Push، روی branch جدا) `lib/db/schema.ts`، `db-web-push-migration.sql`، `lib/notifications/types.ts`/`rules.ts`/`catalog.ts`/`audience.ts`/`service.ts`/`processor.ts`، `lib/notifications/channels/push.ts` (جدید)، `lib/push/subscribeToPush.ts` (جدید)، `app/api/push/subscribe`+`unsubscribe/route.ts` (جدید)، `public/sw-push.js` (جدید)، `components/settings/PushNotificationToggle.tsx` (جدید) + `PreferencesPane.tsx`، `app/(admin)/admin/settings/notifications/page.tsx`، `components/admin/notifications/RecipientDrawer.tsx`، `app/api/admin/notification-rules/route.ts`، `app/api/admin/notification-audience/route.ts`+`options/route.ts`، `app/api/admin/notifications/provider-status/route.ts`، `app/api/admin/notification-outbox/route.ts`، `.env.example`، `project-docs/WEB-PUSH-ROLLOUT.md` (جدید)، تست‌های جدید در `notification-audience.test.ts`/`notification-center-v2.test.ts`
**Build:** فیکس‌های ملی‌پیامک: هر ۵ کامیت جدا (`84cc213`، `455e12f`، `2f70347`، `d4d27fa`، + toggle/sync قبل‌تر) با tsc/تست/lint/build سبز، هر کدوم با تأیید کاربر دیپلوی شد. Web Push روی `feat/web-push-notifications`: tsc ✅ ۰ خطا · tests 475/475 ✅ (۱۴ تست جدید) · build ✅ · lint ✅ (فقط warning‌های preexisting) · git diff --check ✅ · وابستگی جدید: `web-push`+`@types/web-push` (توجیه‌شده — کل قابلیت وابسته به اونه)
**ناتمام:** طبق روال «قبل از push روی main دوباره تأیید بگیر»، Web Push فقط push شد، merge/deploy عمداً انجام نشد. **نکته‌ی مهم:** بین این فیکس‌ها، هارنس یک‌بار push مستقیم به main رو بلاک کرد چون ۳ فیکس پشت‌سرهم بدون سؤال از کاربر push شده بودن (درسته که همه تأیید ضمنی داشتن چون کاربر داشت زنده تست می‌کرد، ولی تأیید صریح گرفته نشده بود) — کاربر بعداً تأیید کرد که مشکلی نبود، ولی از این به بعد باید حتی برای فیکس‌های کوچیک در حین تست زنده هم صریح بپرسم.
**برای جلسه‌ی بعد:** از کاربر تأیید بگیر برای merge/deploy `feat/web-push-notifications`. قبلش کاربر باید ۱) `db-web-push-migration.sql` رو دستی اجرا کنه، ۲) ۴ متغیر VAPID رو در Liara تنظیم کنه. بعد از دیپلوی، تست واقعی از تنظیمات → اعلان‌ها → «نوتیفیکیشن مرورگر» لازمه (بدون دسترسی من به production).

## 📓 2026-07-26 — رفع API ملی‌پیامک: از REST قدیمی (کاربری/رمز) به کنسول توکنی — branch جدا `fix/melipayamak-token-api`
**چه شد:** `feat/product-ui-v2` و `feat/melipayamak-provider` هر دو با تأیید کاربر merge/deploy شدن (هر دو GH Actions سبز، `main` روی `5dac410`). بلافاصله بعدش کاربر پرسید «اطلاعات ملی‌پیامک رو از کجا بردارم؟» و ۳ اسکرین‌شات از پنل واقعی‌ش (`console.melipayamak.com`) فرستاد — اسکرین‌شات سوم («ارسال پیامک ساده») یک API کاملاً متفاوت نشون داد: `POST console.melipayamak.com/api/send/simple/{توکن}` با بدنه‌ی JSON (`from`/`to`/`text`) و پاسخ `{recId, status}` — نه REST قدیمی کاربری/رمزی که طبق دستور اولیه‌ی تسک ساخته بودم (`rest.payamak-panel.com/api/SendSMS/SendSMS`). **نکته‌ی امنیتی مهم همون لحظه:** کاربر یک مقدار با برچسب «pass» مستقیم توی چت پیست کرد — طبق قانون «هرگز credential واقعی رو استفاده/ذخیره نکن»، فوراً بهش گفتم این مقدار استفاده نمی‌شه و توصیه کردم عوضش کنه، هیچ‌جا وارد نشد. با `AskUserQuestion` از کاربر پرسیدم کدوم API رو بسازم — جواب: «توکن جدید» (همینی که در پنل واقعی دیده). **تغییر:** `lib/sms/melipayamak.ts` بازنویسی شد — بدنه از `application/x-www-form-urlencoded` (username/password/to/from/text/isFlash) به `application/json` (`{from,to,text}`) تغییر کرد؛ URL توکن رو مستقیم در مسیر می‌گیره (`MELIPAYAMAK_TOKEN` جایگزین `USERNAME`+`PASSWORD` شد، `MELIPAYAMAK_FROM` بدون تغییر)؛ منطق موفقیت از `RetStatus===1` به «status خالی/غایب + recId مثبت» تغییر کرد (طبق مستندات کم‌جزئیات خود پنل: «status: شرح خطا در صورت بروز» — یعنی خالی=موفق). **ریسک امنیتی مشابه Kavenegar دوباره تکرار شد و رفع شد:** این API هم توکن رو مستقیم در URL می‌ذاره — همون الگوی redaction که برای Kavenegar اضافه کرده بودم، برای این دامنه هم اضافه شد (`lib/notifications/redaction.ts`). `dispatcher.ts`ی `isProviderConfigured` هم به‌روز شد (`MELIPAYAMAK_TOKEN`+`FROM`). همه‌ی تست‌های ملی‌پیامک (بخش قابل توجهی از ۳۰ تست `sms-provider.test.ts` + ۲ تست `provider-status`) با mock جدید بازنویسی شدن؛ `sms-send.test.ts` دست‌نخورده موند (dispatcher رو مستقیم mock می‌کنه). `.env.example` و `MELIPAYAMAK-PROVIDER-ROLLOUT.md` هم کامل به‌روز شدن — راهنمای دقیق «از کجای پنل توکن/شماره‌ی خط رو برداری» اضافه شد.
**فایل‌ها:** (ویرایش) `lib/sms/melipayamak.ts` (بازنویسی کامل adapter)، `lib/sms/dispatcher.ts`، `lib/notifications/redaction.ts` (الگوی جدید)، `.env.example`، `tests/unit/sms-provider.test.ts`، `tests/unit/notification-center-v2.test.ts`، `project-docs/MELIPAYAMAK-PROVIDER-ROLLOUT.md`
**Build:** tsc ✅ ۰ خطا · tests 461/461 ✅ (بدون تغییر در تعداد — فقط بازنویسی محتوای تست‌های ملی‌پیامک) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · git diff --check ✅ · `git diff main -- package.json` خالی
**ناتمام:** هیچ تست شبکه‌ی واقعی با توکن واقعی انجام نشد (بدون دسترسی production). چون مستندات پنل برای شکل دقیق `status` هنگام خطا کامل نیست، منطق تشخیص موفقیت/شکست («status خالی=موفق») باید با **اولین ارسال واقعی** تأیید بشه — این صراحتاً در rollout doc یادداشت شده. push نشده هنوز — commit نکردم.
**برای جلسه‌ی بعد:** این branch رو commit/push کن، از کاربر برای merge/deploy تأیید بگیر. بعد از دیپلوی، وقتی کاربر `MELIPAYAMAK_TOKEN`/`FROM` واقعی رو در Liara تنظیم کرد و یک پیامک واقعی از `/api/sms/test-notify` فرستاد، حتماً چک کن که `sms_log.status='sent'` واقعاً درست تشخیص داده شده (نه false negative/positive روی فرمت response).

## 📓 2026-07-26 — افزودن ملی‌پیامک به‌عنوان provider دوم پیامک — branch جدا `feat/melipayamak-provider`
**چه شد:** طبق دستور صریح، لایه‌ی SMS بازآرایی شد تا از دو provider پشتیبانی کنه (Kavenegar موجود + ملی‌پیامک جدید)، انتخاب فقط با `SMS_PROVIDER`. **معماری:** `lib/sms/types.ts` قرارداد مشترک (`SmsSendOutcome`/`SmsProviderAdapter`) گرفت؛ `lib/sms/melipayamak.ts` adapter جدید (POST به `rest.payamak-panel.com/api/SendSMS/SendSMS`، بدنه‌ی `application/x-www-form-urlencoded` با username/password/to/from/text/isFlash=false؛ `RetStatus===1`→sent با `Value` به‌عنوان messageId)؛ `lib/sms/kavenegar.ts` به همون قرارداد وصل شد، منطق ارسال دست‌نخورده، فقط timeout (`AbortSignal.timeout` ۱۰ ثانیه) و redaction اضافه شد؛ `lib/sms/dispatcher.ts` تابع خالص `resolveSmsProvider()` (SMS_PROVIDER تنظیم‌نشده→kavenegar برای backward compat؛ مقدار نامعتبر→fail-closed بدون هیچ تماس شبکه) + `getSmsProviderStatus()` (بدون افشای secret) + `dispatchSms()`؛ `lib/sms/phone.ts` تابع خالص `normalizeIranPhone()` (۰۹xxxxxxxxx، فرمت‌های +98/0098/98/فارسی). **تصمیم مهم عدم‌تقارن عمدی:** نبودن `KAVENEGAR_API_KEY` طبق رفتار تاریخی و تست‌شده همیشه dry-run بوده (نه خطا) — این حفظ شد چون deploymentهای موجود روش تکیه دارن؛ اما ملی‌پیامک چون تاریخچه‌ای نداره، پیکربندی ناقص‌ش طبق دستور صریح fail-closed می‌شه (نه سقوط بی‌صدا به dry-run). هیچ fallback خودکار بین دو provider نیست (طبق دستور — timeout مبهم می‌تونست باعث ارسال تکراری بشه). `sendSms.ts`: شماره‌ی موبایل حالا اول normalize می‌شه (شماره‌ی نامعتبر→failed بدون لمس‌کردن cap/dedup)؛ سقف روزانه/dedup/dry-run **بدون تغییر** (فقط فراخوانی نهایی از `kavenegarSend` مستقیم به `dispatchSms` عوض شد)؛ **یک باگ واقعی کشف و رفع شد:** ستون `sms_log.provider` از قبل وجود داشت (پیش‌فرض `'kavenegar'`) ولی هیچ‌وقت صریح ست نمی‌شد — یعنی حتی وقتی provider دیگه‌ای اضافه می‌شد، هر ردیف همیشه `'kavenegar'` می‌گفت؛ الان provider واقعی هم روی این ستون هم داخل `providerResponse` ذخیره می‌شه (نیاز به migration نداشت، ستون از قبل بود). **یافته‌ی امنیتی جانبی هنگام تست:** Kavenegar کلید API رو مستقیم داخل مسیر URL می‌ذاره (`/v1/{key}/`) — الگوهای redaction عمومی موجود (`lib/notifications/redaction.ts`) این رو تشخیص نمی‌دادن؛ یک الگوی اختصاصی برای این URL اضافه شد + یک الگوی عمومی‌تر برای `user:pass@` (قبلی فقط `:pass@` رو می‌گرفت، نه یوزرنیم قبلش) — یک تست همین گپ رو مستقیم پیدا کرد. **routeهای readiness:** `provider-status` و `notification-outbox` به‌جای چک مستقیم `KAVENEGAR_API_KEY`، از `getSmsProviderStatus()` استفاده می‌کنن — فیلد جدید `sms.provider`/`summary.smsProvider` اضافه شد، بدون افشای secret؛ رفتار قبلی (backward-compat، SMS_PROVIDER تنظیم‌نشده) دقیقاً حفظ شد (تست‌های قدیمی دست‌نخورده pass شدن).
**فایل‌ها:** (جدید) `lib/sms/dispatcher.ts`، `lib/sms/melipayamak.ts`، `lib/sms/phone.ts`، `tests/unit/sms-provider.test.ts` (۳۰ تست)، `tests/unit/sms-send.test.ts` (۹ تست)، `project-docs/MELIPAYAMAK-PROVIDER-ROLLOUT.md` · (ویرایش) `lib/sms/types.ts`، `lib/sms/kavenegar.ts`، `lib/sms/sendSms.ts`، `lib/notifications/redaction.ts` (۲ الگوی جدید)، `app/api/admin/notifications/provider-status/route.ts`، `app/api/admin/notification-outbox/route.ts`، `components/settings/SmsPane.tsx` (متن راهنما provider-neutral شد)، `.env.example`، `tests/unit/notification-center-v2.test.ts` (۲ تست جدید برای هر دو provider)
**Build:** tsc ✅ ۰ خطا · tests 461/461 ✅ (۴۱ جدید: ۳۰+۹+۲) · build ✅ · lint ✅ (فقط warningهای preexisting بی‌ربط) · playwright --list ✅ ۱۰۳ تست (بدون تغییر) · git diff --check ✅ · `git diff main -- package.json` خالی (صفر dependency جدید) · بدون migration (ستون `provider` از قبل بود)
**ناتمام:** هیچ تماس شبکه‌ی واقعی با API ملی‌پیامک تست نشد (بدون دسترسی production/credential واقعی) — رفتار بر اساس قرارداد رسمی مستندشده با mock کامل تأیید شد. Commit `af03006` روی `feat/melipayamak-provider` push شد؛ merge به main و deploy عمداً انجام نشد، دسترسی production نداشتم.
**برای جلسه‌ی بعد:** کاربر باید `MELIPAYAMAK_USERNAME`/`PASSWORD`/`FROM` واقعی رو در Liara تنظیم کنه و `SMS_PROVIDER=melipayamak` رو بذاره، بعد از طریق `/api/sms/test-notify` یک تست واقعی بزنه (راهنمای کامل در `project-docs/MELIPAYAMAK-PROVIDER-ROLLOUT.md`). قبل از merge/deploy حتماً از کاربر تأیید بگیر.

<!-- archived to project-docs/handoff-archive.md: 2026-07-14 payroll approve + تأیید نهایی فاز ۱ v0.24.0 + 2026-07-14 فاز امنیتی ۱ + 2026-07-15 استقرار جریان تک‌نشست + 2026-07-15 تست رگرسیون P&L + 2026-07-15 جداسازی E2E + 2026-07-15 notification فاز ۱ + 2026-07-16 Notification Center V2 اولیه + 2026-07-16 اصلاح ۱۵‌گانه NC V2 + 2026-07-16 ممیزی مستقل ۹‌بخشی NC V2 + 2026-07-16 تست‌های رفتاری NC V2 + 2026-07-16 تکمیل کامل NC V2 (۱۱ بخش) + 2026-07-17 اصلاح release-blocker + 2026-07-18 NC V2 در production + scheduler داخل‌پروسه + 2026-07-18 پنل Super Admin موبایل‌فرندلی + 2026-07-18 کنترل گیرندگان اعلان + 2026-07-19 release کنترل گیرندگان اعلان + 2026-07-19 رفع باگ بحرانی crash کامل سایت + دانلود دسته‌ای رزومه + 2026-07-23 ESLint در CI + شروع UI/UX Refresh Phase 1 + 2026-07-23 اصلاح متمرکز باگ‌های موبایل و RTL + 2026-07-23 تکمیل UI/UX Refresh Phase 1 + 2026-07-24 Product UI V2 پایه + 2026-07-25 بازطراحی لیست تراکنش‌ها + 2026-07-24 بازطراحی داشبورد + 2026-07-25 merge/deploy کامل feat/product-ui-v2 روی main + 2026-07-25 بازطراحی فرم ثبت تراکنش + 2026-07-25 هاتفیکس‌های فرم ثبت تراکنش + 2026-07-26 بازطراحی کامل استخدام + 2026-07-26 merge/deploy بازطراحی استخدام روی main + 2026-07-26 ۳ اصلاح زنده روی استخدام + 2026-07-26 ممیزی نهایی Product UI V2 -->

