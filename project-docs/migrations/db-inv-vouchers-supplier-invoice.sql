-- ═══════════════════════════════════════════════════════════════════
--  Migration: شماره فاکتور تامین‌کننده روی برگه‌ی رسید انبار (kind='in')
--  یافته‌ی واقعی حین بررسی: ایمپورت گروهی اکسل رسید انبار «شماره فاکتور»
--  را فقط برای گروه‌بندی موقت ردیف‌ها در حافظه استفاده می‌کرد و بعد از
--  ساخت برگه کاملاً دور می‌ریخت — هیچ‌جا ذخیره نمی‌شد. این migration +
--  تغییرات کد همراهش، هم این باگ را رفع می‌کند، هم امکان ثبت دستی شماره
--  فاکتور را به فرم تک‌برگه‌ای اضافه می‌کند.
--  ستون nullable — برگه‌های قدیمی/غیر-خرید بدون این مقدار می‌مانند.
--  Idempotent — اجرای چندباره امن است. هیچ داده‌ای حذف/تغییر نمی‌شود.
--  ⚠️ این فایل روی دیتابیس واقعی اجرا نشده — فقط برای اجرای دستی توسط
--     مدیر پروژه (pgAdmin/DBeaver) پس از تهیه‌ی backup آماده شده است.
-- ═══════════════════════════════════════════════════════════════════

ALTER TABLE inv_vouchers ADD COLUMN IF NOT EXISTS supplier_invoice_no text;

-- ─── تأیید ───
SELECT 'inv_vouchers.supplier_invoice_no column created' AS status;
SELECT COUNT(*) AS in_vouchers_with_invoice FROM inv_vouchers WHERE kind = 'in' AND supplier_invoice_no IS NOT NULL;

-- ═══════════════════════════════════════════════════════════════════
--  بازگشت (Rollback):
--  ALTER TABLE inv_vouchers DROP COLUMN IF EXISTS supplier_invoice_no;
-- ═══════════════════════════════════════════════════════════════════
